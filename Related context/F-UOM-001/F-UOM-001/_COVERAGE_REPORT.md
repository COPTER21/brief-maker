# \_COVERAGE\_REPORT — ทะเบียนหน่วยวัด (F-UOM-001) · **รอบ 1 (HTML)**

> ตรวจ `_final-docs/F-UOM-001/uom-master.html` · 2026-08-10 · **รันซ้ำหลังแก้ตาม Strike**
> คำถามของรอบนี้: **"จอมีทาง/ช่อง/สถานะรองรับครบตาม contract มั้ย"**
> (รอบ 2 จะถาม *"FRD + Test case ผูกเรื่องพวกนั้นครบมั้ย"* — คนละคำถาม)

## เปลี่ยนอะไรจากการตรวจครั้งแรก

feedback ของ Strike (2026-08-10): *"ปรับให้หน้านึง minimum 25 รายการ (แสดงผล list view)"*

| | ครั้งแรก | ตอนนี้ |
|---|:--:|:--:|
| แถว/หน้า | 10 | **25** |
| ทะเบียนตั้งต้น | 19 หน่วย | **35 หน่วย** |
| การแบ่งหน้า | 2 หน้า | 2 หน้า (25 + 10) |

ต้องขยาย mock ตามเพราะ **19 < 25 จะทำให้การแบ่งหน้าหายจากจอ** แล้วพิสูจน์ `FN-19` ไม่ได้
**ผลตรวจไม่เปลี่ยน** — BLOCK = 0 · GAP เดิม 3 ข้อ ไม่มี GAP ใหม่ · scope creep ยังเป็น 0

> ⚠️ `PREBRIEF §8 Mock Data Spec` ยังเขียนว่า *"12 หน่วยครบ 6 หมวด"* — ตอนนี้เป็น **35 หน่วย**
> เป็นการเบี่ยงจากบรีฟที่ **ตั้งใจ** มาจากมติ 2 ครั้ง (6 หมวด → 19 หน่วย · Strike 25 แถว/หน้า → 35 หน่วย)
> ไม่กระทบ scenario ที่ §8 ต้องพิสูจน์ (`S-05` ยังมีตัว `used=0` ให้ลบ 6 ตัว · filter/sort ครบ)
> — ยกให้ BA ปรับถ้อยคำ §8 ที่ step 12

## Contract ที่ใช้เป็นเกณฑ์

| แหล่ง | ใช้ตัดสินอะไร |
|---|---|
| `Brief Feature/2. UOM/PREBRIEF_F-UOM_UoM-Master.md` | OB-1…OB-6 · S-01…S-09 · BR-01…BR-07 · §9 edges · §11 Coverage Matrix |
| `Brief Feature/2. UOM/FUNCTION_CHECKLIST_F-UOM_UoM-Master.md` | **FN-01…FN-19 + FN-13b + FN-90/92/93 (รวม 23) + FN-40 (6 ข้อห้ามมี)** |
| `Central Plan v2/CENTRAL_PLAN_CORE_ERP.md` §2 | Global Contracts #1 · #6 · #7 · #9 |
| `Central Plan v2` §3 Product · §4 master lane | edge ออก 1 เส้น (→ ทะเบียนสินค้า) · edge เข้า 0 เส้น |

> ⚠️ **ไม่มี `workflow_graph.json` ใน Central Plan v2** — `PREBRIEF` ทำหน้าที่แทน NODE_BRIEF
> ตามที่ `CLAUDE.md` กำหนด · `golden_rules` 161 ข้อของ v1 ไม่มีใน v2 → ใช้ **Global Contracts §2** แทน

---

## Verdict

# ⚠️ WARN

| | |
|---|---|
| **BLOCK** | **0** |
| **WARN** | **3** (GAP-01 · GAP-02 · GAP-03) |
| N/A-UI | 1 (`FN-07` — ตรวจฝั่งสินค้า ไม่ใช่จอนี้) |
| scope creep | **0** |
| FN ครอบบนจอ | **22 / 23** (ขาด `FN-07` ตัวเดียว ซึ่งอยู่นอกจอนี้โดยนิยาม) |
| FN-40 ยืนยัน "ไม่มี" | **6 / 6** |

**ไปต่อ step 5 ได้** — WARN ทั้ง 3 ข้อมี precedent และไม่ปิดกั้นการเดินหน้า

---

## 1 · Coverage Matrix — FN ทั้ง 23 ข้อ

> ทุก ✓ มี evidence เป็น route + selector/function ที่ชี้ได้จริง — **ไม่มีช่องไหนติ๊กจากความจำ**

### หมวด 1 · สร้าง + แก้ไข

| FN | HTML | Evidence |
|---|:--:|---|
| **FN-01** สร้างจาก drawer เดียว กรอกแค่ชื่อไทย + หมวด | ✓ | `#/uom/create` → `.drawer.standard` (680px) · ฟอร์มเดียวไม่มี wizard · field 6 ช่อง บังคับ 2 (`ชื่อ (ไทย) *` · `หมวดหน่วยวัด *`) · `saveUnit()` ปล่อยผ่านเมื่อมีชื่อไทย (หมวดมีค่าตั้งต้น `นับจำนวน`) |
| **FN-02** เว้นรหัสว่าง → ออกรหัสอัตโนมัติจากหมวด | ✓ | `autoCode(category)` = 3 ตัวแรกของ id หมวด + เลขรัน 2 หลัก · `while(RAW.some(...code===c))` กันชนของเดิม · `saveUnit`: `finalCode = code \|\| autoCode(cat)` |
| **FN-03** เลือกสถานะได้ 3 ค่าตั้งแต่ตอนสร้าง | ✓ | `#in-status` มี `ร่าง / ใช้งาน / ไม่ใช้งาน` (ค่าตั้งต้น `ใช้งาน`) |
| **FN-04** ฟอร์มไม่มีข้อความคำอธิบายใต้ช่อง | ✓ | นับใน `.drawer-body`: `.field-help` + `.info-tip` + `[data-tip]` = **0** (สอดคล้อง Rule #67.1 hint opt-in) |
| **FN-05** แก้ได้ทุก field ทุกสถานะ + อัปเดตผู้แก้/เวลา | ✓ | `#/uom/edit/:id` ใช้ฟอร์มเดียวกัน ไม่มี field ไหน `disabled` · `saveUnit('edit')` เขียน `updated_by` + `updated_at` · เข้าได้ทั้งจากแถว (`.ra-btn` title=แก้ไข) และจาก view header |

### หมวด 2 · สถานะ (3 ค่า · อิสระ)

| FN | HTML | Evidence |
|---|:--:|---|
| **FN-06** เมนูเปลี่ยนสถานะจาก view · ค่าปัจจุบันกดไม่ได้ · ไม่มีขั้นอนุมัติ | ✓ | `#btnStatus` → `#statusMenu` 3 รายการ · เรนเดอร์จริงได้ `["ร่าง", "ใช้งาน [disabled]", "ไม่ใช้งาน"]` เมื่อ record เป็น *ใช้งาน* · `setStatus()` เขียนทันทีไม่มี gate |
| **FN-07** หน่วยที่ไม่ใช่ *ใช้งาน* ต้องไม่โผล่ใน combobox ของทะเบียนสินค้า | **N/A-UI** | **checklist เขียนเองว่า "(ตรวจฝั่งสินค้า)"** — จอนี้เป็นต้นทาง ไม่มี combobox ให้ตรวจ · สิ่งที่จอนี้ต้องมีคือ *สถานะที่ตั้งได้* + *การสื่อสารผลปลายทาง* → มีครบ (แท็บ **การใช้งาน** แสดงบรรทัด "เลือกได้ในทะเบียนสินค้า: ได้ (สถานะใช้งาน) / ไม่ได้ — แสดงเฉพาะหน่วยที่ใช้งาน") · **ตัวจริงต้องตรวจที่ `F-PDM-001` → ดู GAP-03** |
| **FN-08** ติ๊กหลายแถว (รวมทั้งหน้า) → bulk bar พร้อมจำนวน | ✓ | `.cb` ต่อแถว (`toggleSel`) + `.cb` ในหัวตาราง (`toggleSelPage`) · `.bulk-bar` โผล่เมื่อ `selIds().length > 0` แสดง `เลือก N หน่วย` · ภาพ `_shots/08_bulk_bar.png` |
| **FN-09** bulk ตั้งสถานะ 3 ค่า + toast จำนวน | ✓ | `.bulk-bar` มีปุ่ม `ตั้งเป็น ใช้งาน / ร่าง / ไม่ใช้งาน` → `bulkSetStatus(s)` นับเฉพาะตัวที่เปลี่ยนจริง แล้ว `showToast('เปลี่ยนสถานะ N หน่วย เป็น … แล้ว')` |

### หมวด 3 · ลบ

| FN | HTML | Evidence |
|---|:--:|---|
| **FN-10** bulk ลบ → confirm บอกจำนวนลบจริง + จำนวนที่ข้าม | ✓ | `openBulkDelete()` → modal 440px · นับ `skip = ids.filter(used>0)` แล้วขึ้นข้อความ *"จะลบจริง N รายการ · ข้าม M รายการที่ถูกสินค้าใช้อยู่"* · ปุ่มยืนยันเขียน `ลบ N รายการ` (ตัวเลขหลังหักข้าม) และ `disabled` เมื่อ N = 0 · ภาพ `_shots/09_bulk_delete_confirm.png` |
| **FN-11** ยืนยันแล้ว: `used=0` หาย · `used>0` ยังอยู่ + toast บอกยอดข้าม | ✓ | `confirmBulkDelete()` แยก `del` / `skip` ด้วย `r.used>0` แล้ว `RAW = RAW.filter(...)` · toast *"ลบแล้ว N หน่วย · ข้าม M หน่วยที่ถูกสินค้าใช้อยู่"* (variant `warning` เมื่อมีข้าม) |

### หมวด 4 · Validation

| FN | HTML | Evidence |
|---|:--:|---|
| **FN-12** รหัสซ้ำไม่สนตัวพิมพ์ → บล็อกพร้อมชี้ช่อง ทั้งสร้างและแก้ | ✓ | `saveUnit()`: `RAW.some(r => r.code.toLowerCase()===finalCode.toLowerCase() && r.id!==id)` — `r.id!==id` ทำให้ใช้ได้กับโหมดแก้ไขโดยไม่ชนตัวเอง · `setFieldError('fld-code','err-code','รหัส X ถูกใช้แล้ว')` → `.field.is-invalid` ทำให้ขอบแดง + ข้อความโผล่ |

### หมวด 5 · นำเข้า / ส่งออก

| FN | HTML | Evidence |
|---|:--:|---|
| **FN-13** จอแรกมีแค่กล่องเลือกไฟล์ + ปุ่ม template · **ไม่มีข้อความอธิบายคอลัมน์** | ✓ | `importHTML()` step `pick` = `.upload-box` (`เลือกไฟล์ CSV`) + footer `ดาวน์โหลด template ตัวอย่าง` เท่านั้น · ภาพ `_shots/10_import_pick.png` ยืนยันว่าไม่มีย่อหน้าอธิบาย |
| **FN-13b** ปุ่ม template ได้ไฟล์ header ถูก + แถวตัวอย่างครอบ 3 สถานะ | ✓ | `downloadTemplate()` → header `code,name_th,name_en,symbol,category,status` + 3 แถว: `TON…ใช้งาน` · `SET…` (ว่าง = ร่าง) · `KM…ไม่ใช้งาน` · เขียนด้วย UTF-8 BOM (`﻿`) ผ่าน `downloadCSV()` |
| **FN-14** preview รายแถวมีคอลัมน์สถานะ + BAD_STATUS / BAD_CATEGORY / CODE_DUPLICATE | ✓ | `importError()` คืน 4 กรณี (`REQUIRED` · `BAD_CATEGORY` · `BAD_STATUS` · `CODE_DUPLICATE`) · ตาราง preview 5 คอลัมน์ รวม **สถานะ** · ภาพ `_shots/11_import_preview.png` เห็น `CODE_DUPLICATE — รหัสซ้ำ` (PCS) และ `BAD_STATUS — สถานะไม่ถูกต้อง` (BAG) พร้อมกัน |
| **FN-15** แต่ละแถวได้สถานะตามไฟล์ (ว่าง = ร่าง) · แถวผิดข้าม ไม่ล้มไฟล์ | ✓ | `impConfirm()`: `status: STATUS_BY_TH[row.status] \|\| 'draft'` · `if(row.err) return` (ข้ามทีละแถว ไม่ throw) · ยืนยันจากภาพ `_shots/17_bottom_edge_menu.png` — `TON` เข้ามาเป็น *ใช้งาน* ตามไฟล์ |
| **FN-16** ปิดจอผลแล้ว list + ตัวนับ stat อัปเดต | ✓ | `impConfirm()` เรียก `render()` · เทียบ: `01_list` = **35 รายการ** → หลังนำเข้า = **38 รายการ** · stat *ใช้งาน* 29 → 30 · ยืนยันด้วยเคส `E33` |
| **FN-17** ส่งออก CSV ตาม filter ปัจจุบัน | ✓ | `exportCSV()` ใช้ `getFiltered()` (ไม่ใช่ `RAW`) → เคารพ search + หมวด + สถานะ · 7 คอลัมน์ตาม `S-08` รวม *จำนวนสินค้าที่ใช้* · UTF-8 BOM |

### หมวด 6 · List + ทั่วไป

| FN | HTML | Evidence |
|---|:--:|---|
| **FN-18** คอลัมน์รหัสเป็นตัวหนังสือล้วน ไม่มี icon/avatar | ✓ | `<td><span class="code-cell">CODE</span></td>` — ไม่มี `<i>` / `<img>` / `.uom-thumb` ในคอลัมน์นี้ · ตรวจภาพ `_shots/01_list.png` ยืนยัน |
| **FN-19** ค้นหา + filter หมวด/สถานะ + stat กดกรอง + sort + แบ่งหน้า + empty 2 แบบ | ✓ | ค้นหา `aria-label="ค้นหาหน่วยวัด"` (รหัส/ชื่อไทย/ชื่ออังกฤษ/สัญลักษณ์) · select 2 ตัว (`กรองตามหมวด` 6 ค่า + `กรองตามสถานะ` 3 ค่า) · stat 4 ใบเป็น `<button onclick="quickFilter()">` · sortable **5 คอลัมน์** (`code · name_th · category · used · status`) · pager **25 แถว/หน้า** (2 หน้า) · empty: `ไม่พบรายการที่ค้นหา` + `ยังไม่มีหน่วยวัด` |
| **FN-90** การลบทุกทางผ่าน confirm เสมอ | ✓ | ทางลบมีทางเดียว = `.bulk-bar .btn-danger` → `openBulkDelete()` → modal · **ไม่มีปุ่มลบที่ลบทันที** (grep `RAW.filter` เจอที่เดียวคือใน `confirmBulkDelete()`) |
| **FN-92** validate ชื่อบังคับก่อนบันทึก + กันกดซ้ำ | ✓ | `if(!nameTh)` → `.is-invalid` + toast *"กรุณากรอกข้อมูลให้ครบถ้วน"* (ภาพ `_shots/07_create_validation.png`) · กันกดซ้ำ 2 ชั้น: `if(state.saving) return` + `btn.disabled = true` + `loader-2 spin` + label `กำลังบันทึก…` |
| **FN-93** แท็บประวัติแสดงผู้สร้าง/ผู้แก้ + เวลา | ✓ | แท็บ *ประวัติ* → `.timeline` 2 เหตุการณ์: `แก้ไขล่าสุด` (`updated_by` + `fmtDate(updated_at)`) และ `สร้างรายการ` (`created_by` + `created_at`) · ภาพ `_shots/04_view_history.png` · **ดู UX-08 — เนื้อบางเพราะ PREBRIEF ไม่ได้เก็บ audit รายเหตุการณ์** |

---

## 2 · FN-40 — ยืนยันว่า "ไม่มี" จริง (6/6)

> `qc-coverage` ตรวจได้แค่ระดับ static — **ตัวยืนยันจริงคือ `e2e` (step 5) ที่เรนเดอร์แล้ว assert**
> ตารางนี้จึงเป็น "หลักฐานชั้นแรก" ไม่ใช่คำตัดสินสุดท้าย

| ห้ามมี | ผล | Evidence |
|---|:--:|---|
| conversion factor / ตัวคูณ / precision / หน่วยฐานต่อสินค้า | ✓ ไม่มี | ข้อความบนจอ (`document.body.innerText`) ไม่มีคำว่า `ตัวคูณ` · `conversion` · `factor` · `ความละเอียด` · `precision` · `หน่วยฐาน` เลยสักคำ · โมเดล record มี 12 field ไม่มี field ตัวเลขเชิงแปลงหน่วย · ฟอร์มมี 6 ช่อง ไม่มีช่องตัวเลข |
| ปุ่มลบรายตัว (แถว / view) | ✓ ไม่มี | `.row-actions` ของแถวแรกมีปุ่มเดียว `title="แก้ไข"` · `.drawer-header-actions` = `[แก้ไข, เปลี่ยนสถานะ, ปิด]` · `.drawer-footer` ของ view = `[ปิด]` เท่านั้น |
| ลบหน่วยที่ถูกสินค้าใช้ | ✓ กันไว้ | `confirmBulkDelete()` แยก `used>0` ออกเป็น `skip` ก่อนลบ · modal บอกจำนวนที่จะข้ามล่วงหน้า |
| ขั้นส่งอนุมัติ / สายอนุมัติ | ✓ ไม่มี | ไม่มีคำว่า `ส่งอนุมัติ` · `สายอนุมัติ` · `ผู้อนุมัติ` · `รออนุมัติ` บนจอ · `mk()` เขียน `approver_role/approved_by/approved_at/approval_chain = null` ทุกแถว (**เก็บเงียบตาม OB-3** — ตรวจค่าจริงแล้วเป็น `null` ครบ 4) |
| เพิ่ม/แก้หมวดหน่วยวัด | ✓ ไม่มี | `CATEGORIES` เป็น const 6 ค่า · ไม่มี route/ปุ่ม/ฟอร์มใดแตะมัน · หมวดปรากฏเป็น `<option>` อย่างเดียว |
| icon/avatar หน้ารหัสใน list | ✓ ไม่มี | ดู FN-18 |

---

## 3 · Global Contracts §2 (Central Plan v2)

| # | กติกากลาง | ผล | Evidence / เหตุผล |
|:--:|---|:--:|---|
| **#1** | DOA ครอบเอกสารต้องเซ็น 19 ใบ | ✓ | ทะเบียนหน่วยวัด **ไม่อยู่ในลิสต์ 19 ใบ** → ไม่ต้องมีสายอนุมัติ · แต่ field placeholder 4 ตัวมีครบและเป็น `null` ตาม `OB-3` |
| **#6** | Soft-reference (LD-4C-02) — master เป็น picker ไม่ FK cascade | ✓ | ปลายทางเก็บ **รหัสหน่วย** อย่างเดียว · `used` เป็นค่าอ่านอย่างเดียวที่มาจากทะเบียนสินค้า (ไม่มี UI แก้) · การลบไม่ cascade — ใช้วิธี **ข้าม** แทน |
| **#7** | Append-only audit · soft archive · **ไม่มี hard delete** ทุก module | ⚠️ | **GAP-01** — ดูข้างล่าง |
| **#9** | CI ล็อกตาม generator · 1 module = 1 ไฟล์ SPA | ✓ | `_UX_CHECK_REPORT` verdict PASS · hex ทุกตัวอยู่ใน whitelist · ไฟล์เดียว self-contained |

### Edges (Central Plan v2 §3 Product · §4 master lane)

| ทิศ | คู่ | ต้องมีทางอะไรบนจอ | ผล | Evidence |
|---|---|---|:--:|---|
| **ออก** | → ทะเบียนสินค้า (`หน่วยนับ` · `[config]`) | ส่งรหัสหน่วย + สถานะ · ปลายทางเห็นเฉพาะ *ใช้งาน* | ✓ | สถานะตั้งได้ 3 ทาง (ฟอร์ม · เมนู view · bulk · ไฟล์นำเข้า) · แท็บ **การใช้งาน** สื่อผลปลายทางเป็นภาษาผู้ใช้ *"เลือกได้ในทะเบียนสินค้า: ได้ (สถานะใช้งาน)"* — ไม่มีรหัส edge/ศัพท์ภายในรั่วขึ้นจอ (Rule #81 ✓) |
| **ออก** | → เอกสารซื้อ/ขาย/คลัง (ผ่านสินค้า) | สัญลักษณ์แสดงคู่จำนวน | ✓ | field `สัญลักษณ์` มีทั้งในฟอร์ม · คอลัมน์ในตาราง · แท็บภาพรวม · ไฟล์ส่งออก |
| **เข้า** | — | ไม่มี edge เข้า | ✓ | ตรงกับ §3 (ไม่มีกล่องไหนป้อนเข้า UoM) |
| — | DOA / Notification | ไม่มีสาย + ไม่ emit | ✓ | กระดิ่งเป็น placeholder (`toggleNotif()` แค่ขึ้น toast *"ไม่มีการแจ้งเตือนใหม่"*) ตาม `OB-4` |

---

## 4 · S / BR ตาม PREBRIEF §11

| S | BR | จอ/ปุ่ม | ผล |
|---|---|---|:--:|
| S-01 สร้างหน่วยใหม่ | BR-01, BR-03 | `#/uom/create` | ✓ |
| S-02 แก้ไข | BR-01, BR-07 | `#/uom/edit/:id` | ✓ |
| S-03 เปลี่ยนสถานะรายตัว | BR-03, BR-06, BR-07 | `#statusMenu` | ✓ |
| S-04 เปลี่ยนสถานะหลายตัว | BR-03 | `.bulk-bar` | ✓ |
| S-05 ลบหลายตัว (บางตัวถูกใช้) | BR-02 | `openBulkDelete` + confirm | ✓ |
| S-06 รหัสซ้ำ | BR-01 | ฟอร์ม + นำเข้า | ✓ |
| S-07 นำเข้า CSV พร้อมสถานะในไฟล์ | BR-05, BR-01, BR-03 | modal 3 จังหวะ | ✓ |
| S-08 ส่งออก CSV | — | `exportCSV()` | ✓ |
| S-09 **ไม่รองรับ** 5 ข้อ | BR-04 | ตรวจว่าไม่มี | ✓ (ดู FN-40) |

**BR ครบทั้ง 7 ข้อ · ทุกทางของ state machine (ร่าง ⇄ ใช้งาน ⇄ ไม่ใช้งาน) มีคนกดได้จริง**

---

## 5 · Gap List (3) — เรียงตามความเสี่ยง

### GAP-01 · WARN · Global Contracts §2 #7 — bulk ลบเป็นการลบจริงออกจากทะเบียน

| | |
|---|---|
| ขัดอะไร | GC#7 *"Append-only audit · soft archive · **ไม่มี hard delete** ทุก module"* |
| จอทำอะไร | `confirmBulkDelete()` → `RAW = RAW.filter(...)` — แถวหายจากทะเบียนจริง (ตรงตาม `FN-11` ที่เขียนว่า *"ตัว `used=0` หายจากทะเบียน"*) |
| ตัวกันที่มีจริง | `BR-02` (ถูกสินค้าใช้ = ลบไม่ได้) + confirm modal บอกจำนวนล่วงหน้า + สถานะ *ไม่ใช้งาน* ทำหน้าที่ soft archive อยู่แล้ว |
| **precedent** | `F-PDM-001/_COVERAGE_REPORT.md` ขึ้นข้อเดียวกันเป็น **GAP-01 (WARN ไม่ BLOCK)** และเสนอปรับถ้อยคำ GC#7 ไว้ที่ step 12 — **ยังไม่มีใครเคาะ** |
| ต้องทำอะไรต่อ | ไม่แก้จอ · ยกไปรวมกับข้อเสนอของ F-PDM ที่ `PROPOSALS_outbound.md` — ถ้อยคำที่เสนอ: *"ห้าม hard delete เว้นแต่ record ที่ไม่เคยถูกอ้างอิง + มี tombstone ใน audit"* |

### GAP-02 · WARN · Rule #32.5 — หน้าต่างนำเข้าไม่มีโหมด Replace / Merge

ซ้ำกับ **UX-07** ใน `_UX_CHECK_REPORT` — เหตุผลเดียวกัน (`BR-05` + `BR-01` สั่งพฤติกรรมรายแถว · Replace ขัด GC#7 · `F-PDM-001` ตัดสินเหมือนกัน) → ไม่แก้ ยกเป็นข้อเสนอปรับกฎ

### GAP-03 · WARN · `FN-07` ตรวจไม่จบในรอบนี้ + ปลายทางยังไม่ตรง

| | |
|---|---|
| อะไรหาย | `FN-07` (*หน่วยที่ไม่ใช่ "ใช้งาน" ต้องไม่โผล่ใน combobox ของทะเบียนสินค้า*) **ตรวจบนจอนี้ไม่ได้ตามนิยาม** |
| วัดปลายทางแล้วพบว่า | `_final-docs/F-PDM-001/item-master.html` มี `UOM_MASTER` ของตัวเอง **14 หน่วย · 3 หมวด · ไม่มี field `status` เลย** จึงกรอง *ใช้งาน* ไม่ได้ ทั้งที่ `PREBRIEF_F-PDM §9` เขียนไว้เองว่า *"← UoM Master · หน่วย **active**"* · และหัวเชื้อของ BA (`Brief Feature/1. Item Master/Item-master.html`) **มี `active:true` + `.filter(u=>u.active)` อยู่แล้ว** → เป็น **regression ที่หล่นตอน vibe ใหม่ของ F-PDM** ไม่ใช่ของที่ไม่เคยมี |
| ทะเบียนนี้ต่างจากปลายทางยังไง | 6 หมวด vs 3 หมวด · 19 หน่วย vs 14 หน่วย · ทับกันจริงแค่ 6 รหัส (`PCS BOX KG G L ML`) |
| **มติที่ใช้ในรอบนี้** | **UOM เป็นเจ้าของนิยามหมวด — 6 หมวด** (ผู้ใช้เคาะ 2026-08-10) · ทะเบียนตั้งต้นจึงขยายเป็น **19 หน่วย** เพื่อรองรับ 6 รหัสที่ปลายทางเสนอแต่ทะเบียนไม่เคยมี (`DOZEN · CASE · CARTON · PALLET · JOB · M3`) |
| ต้องทำอะไรต่อ | **นอกขอบเขตรอบนี้** (แตะไฟล์ feature อื่น) → ลง `PROPOSALS_outbound.md` เป็นรายการที่ `F-PDM-001` ต้องทำตอนเดินต่อ: ①`CAT_TH` 3→6 หมวด ②`UOM_MASTER` ยกมาจากทะเบียนนี้ ③คืน field `active` + `.filter(u=>u.active)` ④คืน field `symbol` ⑤แก้ถ้อยคำ `BR-05` ในบรีฟ ⑥ย้อนรัน step 3·4·5 ของ F-PDM |

---

## 6 · Scope Creep — ไม่พบ

ไล่ของบนจอทุกชิ้นเทียบ `PREBRIEF` แล้ว **ไม่มีอะไรเกิน contract**:
`ส่งออก CSV` (S-08) · `นำเข้า CSV` (S-07) · `สร้างหน่วยวัด` (S-01) · `bulk 3 สถานะ + ลบ` (S-04/S-05) ·
`stat 4 ใบ` (§6 *stat card กดกรองเร็ว*) · `sort 5 คอลัมน์` (§6 *sort*) · `แท็บ 3 ตัว` (§6 *tab ภาพรวม/การใช้งาน/ประวัติ*)

---

## 7 · ตัวหารสำหรับ step 5 (e2e)

| | จำนวน |
|---|:--:|
| FN บวก | **23** (FN-01…19 + FN-13b + FN-90/92/93) |
| FN ที่ทดสอบบนจอนี้ได้ | **22** (ยกเว้น `FN-07` — cross-feature) |
| FN-40 (เคสเชิงลบ) | **6** |
| **รวมสิ่งที่ e2e ต้องครอบ** | **28 หัวข้อ** |

---

## NOT-CHECKED (ไม่นับเป็นผ่าน)

| หมวด | เหตุผล |
|---|---|
| FRD Pack | **ยังไม่เกิด** — สร้างที่ step 7 · จะตรวจในรอบ 2 (step 10) |
| Test cases | **ยังไม่เกิด** — สร้างที่ step 9 · จะตรวจในรอบ 2 |
| `FN-07` ฝั่งปลายทาง | ต้องตรวจที่ `F-PDM-001` ซึ่งอยู่นอกขอบเขตรอบนี้ (GAP-03) |
