# 04_DB — F-PURCH-CFG-001 ตั้งค่าการจัดซื้อ (Purchase Configuration)

> **Audience:** DBA · **DB:** PostgreSQL (multi-tenant) · 3 ตารางใหม่ + อ่าน external masters

---

## §4.1 Table Overview
| Table | Owner | Access | Description |
|---|---|---|---|
| **`T_purchase_config`** | #PCFG | R/W | config 6 กลุ่ม (1 แถว/tenant) |
| **`T_doc_numbering`** | #PCFG | R/W | เลขที่เอกสาร ต่อ tenant×doc_type |
| **`T_purchase_config_log`** | #PCFG | append | audit การเปลี่ยน config/numbering |
| `M_payment_term` | Finance | READ | source ของ payment types (FN-10) |
| `T_budget_config` | Budget #4 | READ | release_require_unlock_ref (FN-07) |
| `M_vendor_price_list` | Master | READ | (consumer PR/PO ใช้ autofill) |

## §4.2 T_purchase_config (1 แถว/tenant)
| Column | Type | Null | Class | Description |
|---|---|---|---|---|
| config_id | uuid PK | NO | Internal | |
| tenant_id | uuid UNIQUE | NO | Internal | |
| enable_comparison | boolean | NO | Internal | |
| comparison_required | boolean | NO | Internal | |
| comparison_min_quotes | int | NO | Internal | |
| comparison_threshold_amount | numeric(18,2) | NO | Internal | |
| comparison_award_rule | varchar(10) | NO | Internal | lowest/manual |
| require_pr_before_po | boolean | NO | Internal | |
| pt_enabled | jsonb | NO | Internal | {postpay,prepay,deposit,installment,partial,credit} |
| default_payment_type | varchar(12) | NO | Internal | |
| deposit_default_pct | numeric(5,2) | YES | Internal | (D08) |
| installment_default_terms | int | YES | Internal | (D09) |
| credit_default_days | int | YES | Internal | |
| pr_mandatory | jsonb | NO | Internal | {item,qty,need_by} |
| quote_attachment_required | boolean | NO | Internal | |
| require_approved_vendor_only | boolean | NO | Internal | |
| vendor_pricelist_autofill | boolean | NO | Internal | |
| updated_by | uuid | NO | Internal | |
| updated_at | timestamptz | NO | Internal | |

**Constraints:** UNIQUE(tenant_id) · CHECK comparison_award_rule ∈ ('lowest','manual') · CHECK default_payment_type ∈ pt_enabled keys (app-level VAL-02)

## §4.3 T_doc_numbering (tenant × doc_type)
| Column | Type | Null | Class | Description |
|---|---|---|---|---|
| numbering_id | uuid PK | NO | Internal | |
| tenant_id | uuid | NO | Internal | |
| doc_type | varchar(8) | NO | Internal | PR / CP / PO |
| prefix | varchar(10) | NO | Internal | |
| digits | int | NO | Internal | |
| reset_cycle | varchar(8) | NO | Internal | none/yearly/monthly |
| next_no | int | NO | Internal | |
| last_reset_period | varchar(7) | YES | Internal | YYYY หรือ YYYY-MM (กันรีเซ็ตซ้ำ) |

**Constraints:** UNIQUE(tenant_id, doc_type) · CHECK reset_cycle ∈ ('none','yearly','monthly') · CHECK digits BETWEEN 1 AND 10 · **issue ผ่าน ENG-DOCNUM-01 = SELECT FOR UPDATE (atomic, กันเลขซ้ำ)**

## §4.4 T_purchase_config_log (append-only audit)
| Column | Type | Null | Class |
|---|---|---|---|
| log_id | uuid PK | NO | Internal |
| tenant_id | uuid | NO | Internal |
| changed_at | timestamptz | NO | Internal |
| changed_by | uuid | NO | Internal |
| scope | varchar(12) | NO | Internal (config/numbering) |
| field | varchar(60) | NO | Internal |
| old_value | text | YES | Internal |
| new_value | text | YES | Internal |

**Constraints:** append-only (no UPDATE/DELETE) · index(tenant_id, changed_at)

## §4.5 RLS
- config/numbering/log = scope ต่อ tenant · edit เฉพาะ Admin/Manager (app-level FN-09) · viewer read

## §4.6 Data Classification (v5.1)
| Level | Fields | Enforcement |
|---|---|---|
| **Internal** | ทุก field (config keys, numbering, audit) | default · role gate edit (FN-09) |
| Confidential / PII / Restricted | — (ไม่มี) | — |
- **Policy Center linkage:** ไม่มี Restricted Resource · ไม่มีจำนวนเงินระดับ Confidential (comparison_threshold เป็น config ไม่ใช่ยอดธุรกรรม)
