# _UX_CHECK_REPORT — F-PUR-PR · PR ใบขอซื้อ
**ขั้น 7R · re-gate หลัง vibe** (WF-01 Mode B) · skill `qc-ux-html-checker`
target: `01_HTML/F-PUR-PR.html` · registry F072 · `arch: Q-document` · `dec: doa,ntf,csq,doccfg,pdfdoc` · `auto: lite`
รันเมื่อ 2026-09-13 · Sync Read iron rules #1–#103 จาก `html-generator-v9`

> **รายงานนี้เป็นของไฟล์หลัง vibe** — ไม่ได้คัดลอกจาก `0_DIRECTION/_UX_CHECK_REPORT.md`
> (รายงานต้นน้ำเป็นหลักฐานของไฟล์ก่อน vibe ตาม §7R ข้อ 3 · และรอบนี้พบว่ามันรับรองผิด 2 จุด — ดู §6)

## Verdict: **PASS** (0 BLOCK เหลืออยู่ · 2 WARN) — หลังแก้ BLOCK 1 + WARN 13 จุดในรอบนี้

| ด่าน | ผล |
|---|---|
| BLOCK ที่พบตอนเข้า re-gate | **1** (LOCK #100 — แก้แล้ว) |
| WARN ที่พบตอนเข้า re-gate | **14** (#81 จำนวน 13 จุด แก้แล้ว · #97 1 จุด ไหลต่อ) |
| BLOCK คงเหลือ | **0** |

---

## 1. หลักฐานเชิงกล (รันเองทุกคำสั่ง — ไม่รับ stamp จากต้นน้ำ)

| เช็ค | คำสั่ง | ผล |
|---|---|---|
| JS syntax | สกัด `<script>` 10 บล็อก → `node --check` | ✅ ผ่านทุกบล็อก |
| runtime | Playwright headless 1440×900 · `pageerror` + `console.error` | ✅ **0 / 0** |
| `--z-*` ประกาศค่า | `grep -cE '\-\-z-[a-z]+\s*:'` | **0 ประกาศ / 0 การใช้** → ไฟล์นี้ **ไม่ได้ใช้ z-token indirection** ใช้เลขตรง → **ไม่ติดบั๊ก `z-index:auto`** ที่เจอใน F-SEC-TRANS/F-SEC-LOCKOUT |
| z-index ladder | `grep -oE 'z-index:\s*[0-9]+'` | ✅ 19/20/50/51/60/70/80/90 ไม่ซ้ำ ไม่ชนกัน |
| z-index จริงบนจอ | `getComputedStyle` | ✅ `.drawer-panel` = **51** · `.backdrop` = 50 · `.toast-wrap` = 90 |
| `class` ซ้ำใน tag เดียว | `grep -cE '<[a-z]+ [^>]*class="[^"]*"[^>]*class='` | ✅ **0** |
| ปี ค.ศ. (LOCK §3 #8) | `grep -c '543'` | ✅ **0** — ไม่มี +543 ที่ใดในไฟล์ |
| drawer เปิดจริง | คลิก `tbody tr` → `.drawer-panel.on` | ✅ `true` · width **920** |
| wizard drawer | `.drawer-panel.wide` | ✅ width **1290** (ข้อยกเว้น B2 ตาม #11) |
| responsive sweep | `scrollWidth` vs `innerWidth` @ 1600/1440/1280/1180/1024/900/768 | ✅ **เท่ากันทุกความกว้าง ไม่มี overflow แม้ที่ 768** |

**ภาพหลักฐานทั้งหมดใน `_shots/` (12 ไฟล์ · ถ่ายหลังแก้ B1 และ #81 ทั้งหมด):**

| ไฟล์ | สถานะที่บันทึก |
|---|---|
| `_shots/01_list.png` | หน้ารายการ 5 แถว ครบ 10 คอลัมน์ |
| `_shots/05_wizard_step1.png` | ขั้น 1 เลือกแหล่งที่มา (2 tile) |
| `_shots/05_wizard_step2.png` | ขั้น 2 ข้อมูลหลักใบขอซื้อ (`.form-grid` 2 คอลัมน์) |
| `_shots/05_wizard_step3.png` | **ขั้น 3 `รายการสินค้า`** — หลักฐานว่า B1 แก้แล้ว + ลำดับ totals + B2 v2 grid 9 ช่อง |
| `_shots/05_wizard_step4.png` | ขั้น 4 เอกสารแนบ (`.upload-zone`) |
| `_shots/05_wizard_step5.png` | ขั้น 5 ตรวจสอบและยืนยัน (read-only + สายอนุมัติ) |
| `_shots/04_tab_detail.png` | แท็บรายละเอียด (รวมเอกสารแนบในแท็บนี้) |
| `_shots/04_tab_pdf.png` | แท็บ PDF — `.a4` + จำนวนเงินตัวอักษร + ช่องเซ็น |
| `_shots/04_tab_sign.png` | แท็บลายเซ็น — การ์ดต่อ slot |
| `_shots/04_tab_history.png` | แท็บประวัติ — timeline append-only |
| `_shots/06_empty_filtered.png` | สถานะ "กรองแล้วไม่เจอ" |
| `_shots/07_error_state.png` | สถานะโหลดผิดพลาด + ปุ่ม `ลองใหม่` |

> **ภาพที่ถ่ายก่อนแก้ถูกลบทิ้งทั้งหมด** — ป้องกันไม่ให้ภาพสถานะเก่ากลายเป็นหลักฐานที่ผิด
> (`02_wizard_step1.png` ถ่ายตอนยังเป็น `รายการที่ขอซื้อ` และ `03_view_detail.png` ซ้ำกับ `04_tab_detail.png` — ลบทั้งคู่)

---

## 2. ★ BLOCK ที่พบและแก้ในรอบนี้

### B1 — wizard step 3 ใช้ชื่อผิดจากที่ล็อก (iron rule #100 · HANDOFF §3 ข้อ 1)

| | ค่า |
|---|---|
| พบ | `STEP_NAMES[2] = 'รายการที่ขอซื้อ'` |
| ต้องเป็น | `'รายการสินค้า'` |
| ที่มาของกติกา | `html-generator-v9/knowledge/iron-rules.md:1000` — *"เอกสารธุรกรรมใช้ 5 steps ชื่อ+ลำดับล็อก: ① เลือกแหล่งที่มา · ② ข้อมูลหลัก[เอกสาร] · ③ **รายการสินค้า** · ④ เอกสารแนบ · ⑤ ตรวจสอบและยืนยัน"* · ย้ำอีกครั้งใน `patterns/Q_transaction-document-archetype.md:134` และ `SKILL.md:549` |
| LOCK | HANDOFF §3 ข้อ 1 — *"เปลี่ยนที่นี่ = เอกสารทุกใบใน CUBE ไม่ตรงกัน"* |
| หลักฐานบนจอ | `[...document.querySelectorAll('.stepper .tx')].map(...)` คืน `รายการที่ขอซื้อ` ที่ index 2 |

**แก้ไข** (ตามที่ผู้ใช้ตัดสินใจ — *restore* ค่าที่ล็อก ไม่ใช่การแตะ LOCK):
- `STEP_NAMES[2]` → `'รายการสินค้า'`
- `STEPH('list','รายการที่ขอซื้อ'…)` → `STEPH('list','รายการสินค้า'…)` (หัวขั้น)
- card head ภายในขั้น 3 → `รายการสินค้า`

**ไม่แก้** 2 จุดที่ใช้คำว่า "รายการที่ขอซื้อ" เหมือนกันแต่ **ไม่ใช่ชื่อขั้นของ wizard** จึงไม่อยู่ใต้ #100:
`renderStep5()` การ์ดสรุป (บรรทัด ~1213) · `renderDetailTab()` section (บรรทัด ~1379) — เป็นถ้อยคำเชิง domain ของ PR
ซึ่งสอดคล้องกับหัวตารางอ่านอย่างเดียวที่เขียนว่า `สินค้า / บริการ` (PR รับ `pr_type` = goods|service|asset)

**ยืนยันหลังแก้:** `step3_label_onscreen` = `รายการสินค้า` · `node --check` ผ่าน · 0 pageerror

---

## 3. WARN ที่แก้ในรอบนี้ — Iron Rule #81 (ศัพท์ภายในรั่วขึ้นจอ) · 13 จุด

ทั้งหมดเป็น **ข้อความที่ผู้ใช้เห็นจริง** (toast · field-help · section title) — อยู่ในกรอบ "ถ้อยคำ" ที่ HANDOFF §4 อนุญาตให้ปรับได้
วิธีจับ: ถอด `<style>` + comment ออก แล้ว grep ด้วยรายการคำ `ENG-*` · `mock` · `feature` · `soft-reference` · `My Approval` · `Phase A` · `backend` · รหัสฟีเจอร์ (`F0xx`) · รหัส wave (`W3`/`W7`)
— **static scan จับไม่ได้เลยสักจุด** (`jargon_leak` รู้จักแค่ `E-\d{3}` และคำเฉพาะชุดเดิม) ตรงกับบทเรียน F-SALES-TA

| # | เดิม (ผู้ใช้เห็น) | แก้เป็น |
|---|---|---|
| 1 | `อยู่นอกขอบเขต feature นี้ (W3)` — เมนูใบสั่งซื้อ | `ใบสั่งซื้อยังไม่เปิดใช้งานในระบบช่วงนี้` |
| 2 | `อยู่นอกขอบเขต feature นี้ (F073)` — เมนูเทียบราคา | `การเทียบราคาผู้ขายยังไม่เปิดใช้งานในระบบช่วงนี้` |
| 3 | `อยู่นอกขอบเขต feature นี้ (W3)` — เมนูรับของ | `การรับของยังไม่เปิดใช้งานในระบบช่วงนี้` |
| 4 | `Vendor Master (F007) — soft-reference` | `ทะเบียนผู้ขายอยู่ที่เมนูข้อมูลหลัก — เปิดจากที่นี่ยังไม่ได้` |
| 5 | `Item (F009) — soft-reference` | `ทะเบียนสินค้า/บริการอยู่ที่เมนูข้อมูลหลัก — เปิดจากที่นี่ยังไม่ได้` |
| 6 | `Cost Center (F012) — soft-reference` | `ทะเบียนศูนย์ต้นทุนอยู่ที่เมนูข้อมูลหลัก — เปิดจากที่นี่ยังไม่ได้` |
| 7 | `กล่องรออนุมัติของฉัน — hook My Approval (mock)` | `กล่องรออนุมัติของฉัน — ยังไม่เปิดใช้งานในตัวอย่างนี้` |
| 8 | `ศูนย์แจ้งเตือน — ยิงผ่าน ENG-NOTIFY (mock)` | `ศูนย์แจ้งเตือน — ยังไม่เปิดใช้งานในตัวอย่างนี้` |
| 9 | `ส่งออก CSV — mock (Phase A ยังไม่ต่อ backend)` | `ส่งออกไฟล์ CSV — ยังไม่เปิดใช้งานในตัวอย่างนี้` |
| 10 | field-help: `อ้างอิงแบบอ่อน (soft-reference) จากศูนย์ต้นทุนกลาง — เว้นว่างได้` | `เลือกจากทะเบียนศูนย์ต้นทุนกลาง — เว้นว่างได้` |
| 11 | section title: `การส่งต่อ (soft-reference)` | `การส่งต่อเอกสาร` |
| 12 | ★ ปุ่มในลิ้นชัก: `เทียบราคาผู้ขาย (F073) — ยังไม่อยู่ในขอบเขตรอบนี้` | `การเทียบราคาผู้ขายยังไม่เปิดใช้งานในระบบช่วงนี้` |
| 13 | ★ ปุ่มในลิ้นชัก: `ใบสั่งซื้อ (W3) — ยังไม่อยู่ในขอบเขตรอบนี้` | `การออกใบสั่งซื้อยังไม่เปิดใช้งานในระบบช่วงนี้` |
| + | toast ดาวน์โหลด `— mock` (2 จุด: ไฟล์แนบ · หัวลิ้นชัก) | `ดาวน์โหลดไฟล์ — ยังไม่เปิดใช้งานในตัวอย่างนี้` |
| + | toast `ดาวน์โหลด PDF — mock` | `ดาวน์โหลดไฟล์ PDF — ยังไม่เปิดใช้งานในตัวอย่างนี้` |

★ = **affordance จริงของฟีเจอร์** (ไม่ใช่ scaffolding ของเมนูข้าง) จึงร้ายแรงกว่าข้ออื่น — ตรงกับบทเรียน F-PUR-COMPARE ข้อ 3
คำว่า **"รอบนี้"** ยังถูกถอดออกด้วย เพราะชนกับความหมายเชิง domain (รอบอนุมัติ / รอบเทียบราคา)

**คงไว้โดยตั้งใจ:** `งบไม่พอ (จำลอง)` — ใช้คำไทย "จำลอง" ไม่ใช่ `mock` ✅ ถูกอยู่แล้ว
**ไม่นับเป็นการรั่ว:** `ENG-DOC-NUM` · `ENG-NOTIFY` · `ENG-CSQ` · `TODO:` ที่อยู่ใน **comment ของโค้ด** — ไม่ขึ้นจอ และเป็นที่ที่ contract ควรอยู่

---

## 4. ตรวจ LOCK ทั้ง 10 ข้อของ HANDOFF §3

| # | LOCK | ผล | หลักฐาน |
|---|---|:--:|---|
| 1 | ลำดับ+ชื่อ 5 ขั้น wizard | ✅ *(หลังแก้ B1)* | `STEP_NAMES` = เลือกแหล่งที่มา · ข้อมูลหลักใบขอซื้อ · **รายการสินค้า** · เอกสารแนบ · ตรวจสอบและยืนยัน |
| 2 | 4 แท็บ + เอกสารแนบอยู่ในแท็บรายละเอียด | ✅ | `tabs=[detail,pdf,sign,history]` · `SEC('paperclip','เอกสารแนบ …')` อยู่ใน `renderDetailTab()` · **ไม่มีแท็บแยก** |
| 3 | ตารางรายการ 9 ช่อง + ความกว้าง | ✅ | `<th>` = **26 / auto / 64 / 92 / 92 / 78 / 72 / 104 / 54** ตรงสเปก B2 v2 เป๊ะ |
| 4 | ผู้อนุมัติเป็นคนจริง (รูป+ชื่อ+ตำแหน่ง) | ✅ | `empChip()` → `.avatar`(initials) + `.nm`(ชื่อ) + `.po`(ตำแหน่ง) · ไม่ใช่รหัสบทบาท |
| 5 | ห้าม hardcode วงเงิน/เลขเอกสาร/ช่องทางแจ้งเตือน | ✅ | `MOCK_DOA_TIERS` ถือ `doa_entry_ref: DOA-041/042/043` ชี้ทะเบียนกลาง · `nextDocNo()` มี comment `ENG-DOC-NUM.next('PR')` · แจ้งเตือนเป็น comment `ENG-NOTIFY.emit(...)` พร้อมคำกำกับ *"ห้ามเลือกช่องทางเอง"* |
| 6 | จุดตรวจงบเป็นของจำลอง + TODO | ✅ | `budgetCheckMock()` คืน `{ok,remaining,budget_code,mock:true}` · มี `// TODO: budget-control hook — ENG-BUDGET.check(...) เมื่อ F117 (W7) พร้อม` |
| 7 | soft-ref ไม่บังคับกรอก/ไม่ผูกคีย์ | ✅ | ศูนย์ต้นทุน/ผู้ขาย/สินค้า/หน่วยนับ เป็น `<select>`/combobox ที่มีค่าว่างได้ · ไม่มี `required` · ไม่มี FK validate |
| 8 | ปี ค.ศ. ทุกจุด | ✅ | `grep -c '543'` = **0** |
| 9 | ประวัติเพิ่มอย่างเดียว ห้ามมีปุ่มลบ | ✅ | มีแต่ `pushAudit()` (10 จุด) · ไม่มี `deleteAudit`/ปุ่มลบประวัติ |
| 10 | ห้ามสร้างจอ PO/รับของ/เทียบราคา/ควบคุมงบ | ✅ | `grep -c 'renderPoDrawer|renderGrnDrawer|renderCompare|renderBudget'` = **0** · ปลายทางเป็นปุ่มที่ขึ้น toast เท่านั้น |

---

## 5. ตรวจ DIVERGENCE เทียบ registry (§7R ข้อ 4)

registry F072 ประกาศ `arch: Q-document` + `dec: doa, ntf, csq, doccfg, pdfdoc` (ครบ 5 ชิป)

| surface ที่พบในจอ | registry ประกาศไว้ไหม | ผล |
|---|---|---|
| wizard 5 ขั้น · เลขที่เอกสาร · view drawer 4 แท็บ · line editor B2 | `arch: Q-document` ✅ | ไม่ใช่ DIVERGENCE |
| slot picker + สายอนุมัติตามวงเงิน | ชิป `doa` ✅ | ไม่ใช่ DIVERGENCE |
| comment `ENG-NOTIFY.emit` 5 จุด | ชิป `ntf` ✅ | ไม่ใช่ DIVERGENCE |
| comment `ENG-CSQ doc.approved / doc.cancelled` | ชิป `csq` ✅ | ไม่ใช่ DIVERGENCE |
| `nextDocNo()` → `ENG-DOC-NUM.next('PR')` | ชิป `doccfg` ✅ | ไม่ใช่ DIVERGENCE |
| แท็บ PDF `.a4` + ช่องเซ็น + จำนวนเงินตัวอักษร | ชิป `pdfdoc` ✅ | ไม่ใช่ DIVERGENCE |

**ผล: ไม่พบ DIVERGENCE** — ทุก surface บนจอมีชิป/archetype รองรับ และไม่มีชิปไหนที่ประกาศแล้วไม่มีของบนจอ
หมายเหตุ: `static_scan.doc_archetype.is_document = true` ในเคสนี้ **ถูกต้อง** (ต่างจาก F-PUR-COMPARE ที่เป็น false positive) เพราะฟีเจอร์นี้เป็นเอกสารธุรกรรมจริงตาม `arch`

---

## 6. ★ จุดที่รายงานต้นน้ำ (S3a) รับรองไว้ไม่ตรงกับไฟล์จริง

| ต้นน้ำเขียนว่า | ตรวจจริงพบ |
|---|---|
| *"wizard 5 steps ชื่อตรง: … › **รายการสินค้า** › … ✅ ตรงเป๊ะ"* | ❌ ไฟล์เป็น `รายการที่ขอซื้อ` — **BLOCK B1** |
| *"microcopy ภาษาคน ไม่มีศัพท์ระบบ ✅"* | ❌ พบศัพท์ภายในรั่วขึ้นจอ **13 จุด** (§3) |

> ยืนยันบทเรียนเดิม: **ห้ามยกรายงานของต้นน้ำมาใช้แทนการรันเอง** — ทั้งสองข้อนี้เป็นสิ่งที่ต้นน้ำติ๊ก ✅ ไว้แล้ว

---

## 7. WARN ที่ไหลต่อ (ไม่ block)

- **W1 · #97 responsive** — `body_minwidth_768 = false` และ sidebar กว้างคงที่ 232px ไม่ยุบเป็น rail
  วัดจริงแล้ว **ไม่มี horizontal overflow ที่ 768** (`scrollWidth == innerWidth` ทุกความกว้างที่วัด) จึงจัดเป็น WARN ตาม `check-methods.md` ไม่ใช่ BLOCK
- **W2 · ปุ่มสาธิต 2 ปุ่ม** — `ทดสอบสถานะโหลดผิดพลาด` (หน้ารายการ) · `ทดสอบไฟล์ไม่ผ่านเงื่อนไข` (ขั้นเอกสารแนบ)
  **ตัดสินใจ: เก็บไว้** เพราะ QA ต้องใช้กดเข้าสถานะผิดพลาดสำหรับ TC ของ SC-11 และ SC-15
  → บันทึกเป็นเงื่อนไขบังคับใน `README.md` และ `03_FRD/07_LOCKED_DECISIONS.md` ว่า **ต้องถอดออกก่อนขึ้น production**
- **W3 · `MOCK_DOA_TIERS` ถือตัวเลข 50,000 / 500,000** — เป็นค่า `[ASSUMED]` ของ **OQ-01** ที่ยังรอพี่เบิร์ดยืนยัน
  ไม่ผิด LOCK §3 ข้อ 5 เพราะผูก `doa_entry_ref` ชี้ทะเบียน DOA กลาง แต่ **ต้องไม่ถูกอ่านเป็นค่าจริง** → ยกเข้า `07_LOCKED_DECISIONS`
