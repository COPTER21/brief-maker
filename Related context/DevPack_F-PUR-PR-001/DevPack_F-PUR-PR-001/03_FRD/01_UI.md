# 01_UI — F-PUR-PR · PR ใบขอซื้อ

> Audience: FE dev · **source of truth = `01_HTML/F-PUR-PR.html`** (ผ่าน re-gate ขั้น 7R)
> ทุกบรรทัดในไฟล์นี้ **สกัดจากจอจริง** — ห้ามเขียน spec สวนจอ (R14)
> คู่กับ `HTML_UI_BRIEF_F-PUR-PR-001.md` (anatomy ละเอียดระดับ selector)

## §1.0 ★ Layout Decision Log (Mode A — Recognize & Validate)

HTML ผ่าน gate มาแล้ว → FRD **สกัด + ตรวจ + บันทึก** pattern ที่จอใช้จริง

| Page | route | BRD §14.6 บอก | Pattern (**observed**) | evidence (ฟังก์ชัน/selector จริง) | Deviation |
|---|---|---|---|---|---|
| P-01 | `#/list` | SC-1 หน้ารายการ | **A** (list-view) + landing ของ Q | `renderList()` · `<table>` 10 คอลัมน์ · การ์ด `เอกสารแนบล่าสุด` ท้ายหน้า | — |
| P-02 | `#/create` · `#/edit/:id` | SC-2 wizard | **Q wizard** (drawer กว้าง) + **Stepper #47** | `renderCreateDrawer()` · `.drawer-panel.wide` วัดจริง **1290px** · `.stepper` + `stepItem()` · `STEP_NAMES` 5 ขั้น | ความกว้าง 1290 = **ข้อยกเว้นเดียวของ #11** ที่ B2 อนุญาต |
| P-02.3 | `#/create` ขั้น 3 | SC-2.3 B2 v2 | **B2 v2** (document line editor) | `.line-tbl` · `<th>` 9 ช่อง **26/auto/64/92/92/78/72/104/54** · `renderLineExpand()` · `renderLineSummary()` | — |
| P-03 | `#/view/:id` | SC-3 ดูเอกสาร | **C + G + H + I ครบชุด** (view drawer เอกสาร) | `renderViewDrawer()` · `.drawer-panel` วัดจริง **920px** · `setTab()` 4 แท็บ | ใช้ G+H+I ครบชุดตาม Doc signal — **ห้ามใช้ C generic** |
| P-04 | overlay บน `#/create` | SC-4 ส่งอนุมัติ | **D** (modal) | `renderSubmitModal()` · `.slot-row` + `empChip()` | — |
| P-05 | overlay | SC-5 ไม่อนุมัติ/ยกเลิก | **D** (modal) | `renderReasonModal()` · บังคับเหตุผล ≥3 ตัวอักษร | — |
| P-06 | overlay | SC-6 ยืนยันย้อนกลับไม่ได้ | **D** (modal 440) | `renderModal()` · `440px` ใน px watchlist | — |

**ผลตรวจ:** ทุก pattern ที่จอใช้ compliant กับ `html-generator-v9` · **ไม่มี `⚠️ HTML-STANDARD CONFLICT`** · **ไม่มี `⚠️ HTML-BRD DRIFT`** (BRD §14.6 SC-1..SC-6 ตรงกับจอครบ)

### Shell (ทุกหน้า)
| องค์ประกอบ | ค่าที่วัดได้จริง |
|---|---|
| Sidebar | **232px** · Charcoal flat (ไม่ gradient) · active = `ใบขอซื้อ (PR)` |
| Shell bar | **52px** · พื้นขาว |
| Content padding | 22px |
| Breadcrumb | `จัดซื้อ › ใบขอซื้อ` (list) · `สร้างเอกสารใหม่` (wizard) |
| CI | CUBE Warm Light — Ivory `#FAF8F5` พื้น · Charcoal `#111` sidebar · Red CTA · Orange accent |
| Font | Satoshi + Noto Sans Thai |
| Icon | **Lucide เท่านั้น** (`data-lucide` 95 จุด) · ไม่มี emoji สี |
| z-index ladder | 19/20 (sticky) · **50** backdrop · **51** drawer · 60 modal · 70 combobox pop · 80 · **90** toast |

> **Responsive:** วัด `scrollWidth` vs `innerWidth` ที่ 1600/1440/1280/1180/1024/900/768 → **เท่ากันทุกความกว้าง ไม่มี horizontal overflow**
> sidebar ไม่ยุบเป็น rail ที่ 768 (WARN #97 · ไม่ block — ดู `_UX_CHECK_REPORT` W1)

---

## §1.1 P-01 · หน้ารายการใบขอซื้อ — `#/list`

### ตาราง (10 คอลัมน์ · lean 1 บรรทัด/เซลล์ ตาม #103)
| # | หัวคอลัมน์ (verbatim) | เรียงได้ | หมายเหตุ |
|---|---|:--:|---|
| 1 | `เลขที่` | ✓ `sortBy('code')` | ร่างยังไม่มีเลข → แสดงข้อความแทน |
| 2 | `วันที่เอกสาร` | ✓ `sortBy('docDate')` | ค.ศ. |
| 3 | `ผู้ขอซื้อ` | — | avatar + ชื่อ (`empChip`) |
| 4 | `หน่วยงาน` | — | |
| 5 | `ต้องการใช้` | ✓ `sortBy('requiredDate')` | |
| 6 | `สถานะ` | — | `docPill()` chip |
| 7 | `ประเภท` | — | goods/service/asset |
| 8 | `ลายเซ็น` | — | `renderSignProgress()` n/N + bar 46px |
| 9 | `ยอดสุทธิ` | ✓ `sortBy('grand')` | ชิดขวา + tabular-nums |
| 10 | (actions) | — | width 40px |

### Toolbar / Filter
`LIST.q` (ค้นหา) · `LIST.status` · `LIST.dept` (จาก `MOCK_DEPT`) · `LIST.requester` (จาก `MOCK_EMP`) · ปุ่ม `resetF()` · ปุ่มหลัก **สร้างใบขอซื้อ**

### States (ครบ 4)
| State | trigger | หลักฐานภาพ |
|---|---|---|
| loading | `LIST.loading` → skeleton | — |
| empty (ยังไม่มีข้อมูล) | รายการว่าง | — |
| empty (กรองแล้วไม่เจอ) | `LIST.q` ไม่ match | `_shots/06_empty_filtered.png` |
| error | `LIST.error` → ปุ่ม `ลองใหม่` + `retryLoad()` | `_shots/07_error_state.png` |

> **แถบเอกสารแนบใน landing** (Pattern Q #98): การ์ด `เอกสารแนบล่าสุด` ท้ายหน้า รวมไฟล์จากทุกใบ
> **ปุ่มสาธิต** `ทดสอบสถานะโหลดผิดพลาด` มุมขวาบน — **ต้องถอดก่อน production** (OQ-09)

---

## §1.2 P-02 · Wizard สร้าง/แก้ไข — `#/create` · `#/edit/:id`

### ★ 5 ขั้น — ชื่อและลำดับ **ล็อกโดย iron rule #100** (LOCK · HANDOFF §3 ข้อ 1)

| # | ชื่อขั้น (verbatim · ล็อก) | เนื้อหา |
|---|---|---|
| 1 | **เลือกแหล่งที่มา** | 2 tile: `สร้างใหม่` / `จากใบขอซื้อเดิม` (คัดลอกหัว+รายการ · **ไม่คัดลอกเลขที่ ลายเซ็น ประวัติ**) |
| 2 | **ข้อมูลหลักใบขอซื้อ** | `.form-grid` 2 คอลัมน์ — ผู้ขอ(auto) · หน่วยงาน\* · วันที่เอกสาร\* · วันที่ต้องการใช้\* · ประเภท\* · ศูนย์ต้นทุน(soft) · เหตุผล\* · **ผู้ขายที่แนะนำ**(soft · หลายราย) · อ้างอิงภายนอก · หมายเหตุ |
| 3 | **รายการสินค้า** | B2 v2 ทั้งชุด (ดู §1.3) |
| 4 | **เอกสารแนบ** | `.upload-zone` · PDF/JPG/PNG/XLSX ≤10MB · ลบได้เฉพาะร่าง |
| 5 | **ตรวจสอบและยืนยัน** | read-only ทั้งหมด + แถบตรวจงบ(จำลอง) + สายอนุมัติที่ระบบจะใช้ → `บันทึกแบบร่าง` / `บันทึกและส่งอนุมัติ` |

> ⚠️ **ค่าที่ล็อกคือ `รายการสินค้า`** — ไฟล์ที่รับมาใช้ `รายการที่ขอซื้อ` ซึ่งขัด #100 · **แก้กลับแล้วในขั้น 7R** (ดู `_UX_CHECK_REPORT` B1 · `07_LOCKED` LD-08 · OQ-08)
> Stepper: ขั้นที่ผ่านแล้วคลิกย้อนได้ · ขั้นปัจจุบันและขั้นอนาคตคลิกไม่ได้ (`stepItem()`)

### Validation ต่อขั้น (`wizardNext()`)
| ขั้น | เงื่อนไขผ่าน | ข้อความเมื่อไม่ผ่าน (verbatim) |
|---|---|---|
| 1 | เลือกแหล่งที่มา (+ ใบต้นทางถ้าเลือกคัดลอก) | `กรุณาเลือกแหล่งที่มาก่อน` / `กรุณาเลือกใบขอซื้อต้นทาง` |
| 2 | หน่วยงาน · วันที่เอกสาร · วันที่ต้องการใช้(≥วันที่เอกสาร) · เหตุผล | `กรุณากรอกข้อมูลให้ครบถ้วน` + `markErr()` ทำเครื่องหมายช่องที่ขาด |
| 3 | ≥1 รายการครบ · ไม่มีแถวว่าง · ไม่ซ้ำสินค้า+หน่วย | `ต้องมีอย่างน้อย 1 รายการที่ระบุสินค้า จำนวน และราคาครบ` / `มีแถวว่าง N แถว — กรุณาระบุรายการหรือลบแถวออก` / `มีสินค้าและหน่วยซ้ำกัน: X — รวมเป็นแถวเดียว` |
| 4 | ไม่บังคับ | — |

---

## §1.3 P-02.3 · B2 v2 Line Editor (LOCK-B2)

### ★ Grid 9 ช่อง — ความกว้างล็อก (HANDOFF §3 ข้อ 3)
| # | หัว (verbatim) | width | จัด |
|---|---|---|---|
| 1 | `#` | **26px** | กลาง |
| 2 | `สินค้า` | **auto** | ซ้าย · combobox #102 + พิมพ์เอง |
| 3 | `จำนวน` | **64px** | ขวา |
| 4 | `หน่วย` | **92px** | ซ้าย |
| 5 | `ราคา/หน่วย` | **92px** | ขวา |
| 6 | `ส่วนลด %` | **78px** | ขวา |
| 7 | `ภาษี` | **72px** | กลาง |
| 8 | `จำนวนเงิน` | **104px** | ขวา |
| 9 | (ลบ/ขยาย) | **54px** | — |

`.line-tbl` font-size **12.5px** · แถวขยาย = lean 1 บรรทัด (ภาษี segmented · หมายเหตุ · ศูนย์ต้นทุนรายบรรทัด) · ขยายทีละแถว

### ลำดับ block ใต้ตาราง (ล็อก)
`ตาราง` → `ส่วนลดท้ายบิล` (toggle + segmented) → `หักภาษี ณ ที่จ่าย (ประมาณการ)` (toggle · ฝั่งจ่าย) → `สรุปยอด`

### ลำดับ Totals (ล็อก · `renderLineSummary()`)
`ราคาก่อน VAT` → `ภาษีมูลค่าเพิ่ม (VAT)` *(สีม่วง)* → `ยอดรวมหลัง VAT` → `ส่วนลดท้ายบิล` → **`ยอดสุทธิทั้งหมด`** *(16px/700 · primary)* → บรรทัดธุรกิจ (hook)

### VAT 3 โหมด (ใช้ร่วมกันไม่ได้)
`none` (ไม่คิด) · `add` (ยังไม่รวมภาษี → บวกเพิ่ม) · `included` (รวมภาษีแล้ว → **ถอดภาษีย้อน**) · `vat_pct` ค่าเริ่มต้น 7

---

## §1.4 P-03 · หน้าดูเอกสาร — `#/view/:id`

Drawer **920px** · `setTab()` · **4 แท็บ ลำดับล็อก** (HANDOFF §3 ข้อ 2)

| # | แท็บ (verbatim) | เนื้อหา | ภาพ |
|---|---|---|---|
| 1 | `รายละเอียด` | header KV + ตารางรายการ read-only + totals + ผู้ขายที่แนะนำ + **เอกสารแนบ (อยู่ในแท็บนี้ ไม่ใช่แท็บแยก)** + `การส่งต่อเอกสาร` + ปุ่มตามสถานะ | `_shots/04_tab_detail.png` |
| 2 | `PDF` | `.a4` (max 720) ตาม print spec · จำนวนเงินตัวอักษร (`bahtText()`) · ช่องลายเซ็น · ปุ่มพิมพ์/ดาวน์โหลด | `_shots/04_tab_pdf.png` |
| 3 | `ลายเซ็น / อนุมัติ` | การ์ดต่อ slot: avatar + ชื่อ + ตำแหน่ง + สถานะ + วันเวลา + เหตุผล | `_shots/04_tab_sign.png` |
| 4 | `ประวัติ` | timeline **เพิ่มอย่างเดียว** — `ประวัติ (บันทึกเพิ่มอย่างเดียว · แก้ไข/ลบไม่ได้)` | `_shots/04_tab_history.png` |

### ปุ่มตามสถานะ (`canActStep()`)
| สถานะ | ปุ่มที่เห็น |
|---|---|
| ร่าง | แก้ไข · ส่งอนุมัติ · ยกเลิกเอกสาร · ทำสำเนาเป็นใบใหม่ |
| รออนุมัติ | **ผู้อยู่ในขั้นปัจจุบันเท่านั้น**: อนุมัติ · ไม่อนุมัติ · / ผู้ขอ: เรียกคืน · ทุกคน: ทำสำเนา |
| อนุมัติแล้ว | ยกเลิกเอกสาร · ทำสำเนา · การส่งต่อเอกสาร (เทียบราคา/ใบสั่งซื้อ — ยังไม่เปิดใช้) |
| ไม่อนุมัติ | ทำสำเนาเป็นใบใหม่ |
| ยกเลิก | ทำสำเนาเป็นใบใหม่ |

> **SC-10:** ผู้ที่ไม่อยู่ในขั้นปัจจุบัน → `canActStep()` คืน `-1` → **ไม่ render ปุ่มอนุมัติ** + มี note-box อธิบาย
> **SC-08:** route `#/edit/:id` ของใบที่ไม่ใช่ร่าง → **เด้งกลับ view** + note-box บอกให้กด `เรียกคืน` ก่อน

---

## §1.5 Overlays

| Overlay | ฟังก์ชัน | dismiss | บังคับ |
|---|---|---|---|
| Combobox สินค้า | `.combo-pop` z70 | Esc · คลิกนอก | — |
| ส่งอนุมัติ (slot picker) | `renderSubmitModal()` | Esc · ปุ่มปิด | เลือกครบทุกขั้น ไม่งั้นปุ่มส่งถูกปิด (V-08) |
| เหตุผล (ไม่อนุมัติ/ยกเลิก) | `renderReasonModal()` | Esc *(ยกเว้น reject ที่ปิดไม่ได้)* | เหตุผล ≥3 ตัวอักษร (V-07) |
| ยืนยัน (destructive) | `renderModal()` 440px | Esc | — |

**Esc chain:** combobox → submit modal → reason modal → confirm modal → wizard → view drawer

### ★ Slot picker (LOCK-DOA · HANDOFF §3 ข้อ 4)
`empChip()` = `.avatar`(อักษรย่อ) + `.nm`(ชื่อ) + `.po`(**ตำแหน่ง**) — **ต้องเป็นคนจริง ห้ามแสดงรหัสบทบาทลอย**
จำนวนขั้นมาจาก `resolveDoa(ยอดสุทธิ)` → 1 / 2 / 3 ขั้น ตามช่วงวงเงินของทะเบียน DOA

---

## §1.6 Microcopy verbatim (สำหรับ 06_TESTS)

| บริบท | ข้อความจริงบนจอ |
|---|---|
| โหลดสำเร็จ | `โหลดรายการสำเร็จ` |
| กรอกไม่ครบ | `กรุณากรอกข้อมูลให้ครบถ้วน` |
| ยังไม่เลือกแหล่งที่มา | `กรุณาเลือกแหล่งที่มาก่อน` |
| ยังไม่เลือกใบต้นทาง | `กรุณาเลือกใบขอซื้อต้นทาง` |
| ไม่มีรายการครบ | `ต้องมีอย่างน้อย 1 รายการที่ระบุสินค้า จำนวน และราคาครบ` |
| มีแถวว่าง | `มีแถวว่าง {N} แถว — กรุณาระบุรายการหรือลบแถวออก` |
| ซ้ำสินค้า+หน่วย | `มีสินค้าและหน่วยซ้ำกัน: {ชื่อ} — รวมเป็นแถวเดียว` |
| ยังไม่พิมพ์ชื่อรายการ | `พิมพ์ชื่อรายการก่อน` |
| แนบไฟล์สำเร็จ | `แนบไฟล์แล้ว` |
| ไฟล์ไม่ผ่าน | `ไฟล์ "{ชื่อ}" ({ขนาด}) แนบไม่ได้ — รองรับเฉพาะ PDF · JPG · PNG · XLSX ขนาดไม่เกิน 10 MB ต่อไฟล์ (ไฟล์อื่นที่แนบไว้ยังอยู่ครบ)` |
| เรียกคืนสำเร็จ | `เรียกคืนใบขอซื้อแล้ว — แก้ไขได้` |
| งบไม่พอ | pill `งบไม่พอ (จำลอง)` |
| ประวัติ (หัวข้อ) | `ประวัติ (บันทึกเพิ่มอย่างเดียว · แก้ไข/ลบไม่ได้)` |
| เรียกคืน (คำอธิบาย) | `ใบจะกลับเป็นแบบร่างเพื่อให้แก้ไขได้ ลายเซ็นที่อนุมัติไปแล้วจะถูกยกเลิก (ยังคงบันทึกไว้ในประวัติ) และต้องส่งอนุมัติใหม่ทั้งสาย` |
| ไม่อนุมัติ (คำอธิบาย) | `ระบุเหตุผลให้ผู้ขอทราบ — เหตุผลจะถูกบันทึกถาวรในประวัติและแจ้งไปยังผู้ขอ` |
| ยกเลิก (คำอธิบาย) | `การยกเลิกย้อนกลับไม่ได้ — ใบนี้จะไม่ถูกนำไปออกใบสั่งซื้อ และเหตุผลจะถูกบันทึกถาวรในประวัติ` |
| ปลายทางยังไม่เปิด | `การเทียบราคาผู้ขายยังไม่เปิดใช้งานในระบบช่วงนี้` · `การออกใบสั่งซื้อยังไม่เปิดใช้งานในระบบช่วงนี้` |

> ข้อความ 4 รายการสุดท้ายกลุ่ม "ปลายทาง/เมนู" **ถูกแก้ในขั้น 7R** เพื่อถอดศัพท์ภายใน (iron rule #81) — ดู `_UX_CHECK_REPORT §3`
