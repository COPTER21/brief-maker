# Design System Pending Registry

บันทึกปัญหา BASE-KIT, CI token และข้อเสนอที่ต้องส่งกลับเจ้าของ design system
โดยเก็บหลักฐานจากตัวตรวจและตำแหน่ง artifact ที่พบจริง

| ID | วันที่ | Feature | ประเภท | รายละเอียด | หลักฐาน | สถานะ |
|---|---|---|---|---|---|---|
| DSP-01 | 2026-09-03 | F-HR-RECRUIT | BASE-KIT z-index | `.modal-backdrop` ใน BASE-KIT ใช้ `var(--z-backdrop)` (50) → modal ที่เปิด**ในลิ้นชัก (drawer z=55)** จมอยู่ใต้ drawer มองไม่เห็น/กดไม่ได้ · ผู้ใช้เจอเอง ตัววัดเดิมจับไม่ได้ | แก้ที่ `outputs/F-HR-RECRUIT/สรรหา.html` (`.modal-backdrop{z-index:var(--z-portal)}` =60) · เพิ่มตัวตรวจ `JS_MODAL_UNDER_DRAWER` เข้า `outputs/_SHARED/_e2e/uikit.py` (assert modal z > drawer z ขณะเปิดซ้อน · พิสูจน์ด้วยการย้อนโค้ดพังแล้วจับได้ · C3.8) · e2e case `c_modal_z` (UI-REG) | **รอเจ้าของ design system** แก้ที่ BASE-KIT ต้นทาง (ตอนนี้แก้เฉพาะสำเนา feature — จุดเดียวกันจะหลุดใน feature ถัดไปถ้าไม่แก้ต้นทาง) · **⟳ เกิดซ้ำ F-HR-WELFARE 2026-09-08** (e2e U01 จับได้ · แก้สำเนา `outputs/F-HR-WELFARE/welfare.html` เหมือนเดิม) — ยืนยันว่ายังไม่แก้ต้นทาง · **⟳ เกิดซ้ำ F-HR-Performance 2026-09-09** (e2e DSP-01 จับได้ · แก้สำเนา `outputs/F-HR-Performance/performance.html` `.modal-backdrop{z-index:var(--z-portal)}` เหมือนเดิม) — ยืนยันว่ายังไม่แก้ต้นทาง · เชื่อม [[html-v7-basekit-latent-bugs]] |
| DSP-02 | 2026-09-03 | F-HR-TRAIN | BASE-KIT focus × combobox + disabled tooltip | 3 คลาสบั๊กจาก manual test ที่ตัววัดหลุด: (a) **combobox `onSelect`→`render()` + focus-restore (#29) เด้งโฟกัสกลับเข้า ss-input → `onfocus=ssOpen` เปิด dropdown ซ้ำ** (BUG-02 · เลือกแล้ว list ค้าง input ว่าง) · (b) **`trapFocus` auto-focus ช่องแรกของ modal ถ้าเป็น ss-input → dropdown เด้งเปิดเองตอน modal เปิด** (BUG-04) · (c) **ปุ่ม `disabled`+`pointer-events:none` → `title` tooltip ไม่ขึ้น** (BUG-03) | แก้เฉพาะสำเนา `outputs/F-HR-TRAIN/อบรม.html` (a) blur ก่อน render (b) rAF ปิด dropdown+blur หลัง trapFocus (c) ครอบ `<span class="tt-wrap" title>` · **เพิ่ม 3 uikit helper append-only** (`nested_cards` · `combobox_state_after_select`/`assert_combobox_closes_after_select` · `modal_autoopens_comboboxes`) พิสูจน์ด้วย temp-break in-page (C3.8) · e2e `c_bug01..04` | **รอเจ้าของ design system** พิจารณาแก้ต้นทาง: combobox เปิดบน click แทน focus (ทน focus-restore/trapFocus) · หรือ trapFocus ข้าม ss-input · disabled-affordance ที่โชว์เหตุผลได้ — ตอนนี้แต่ละ feature ต้องแก้เอง · **⟳ เกิดซ้ำ F-HR-WELFARE 2026-09-08** (bug (a) combobox ไม่ปิดหลังเลือก + (b) modal เปิดแล้ว dropdown กางเอง — **ผู้ใช้เจอเอง เพราะ e2e-welfare ไม่ได้เรียก uikit check DSP-02 ที่มีอยู่แล้ว**) · แก้สำเนา welfare.html (ssBlurActive + guardOverlayAutoCombo) + wire check เข้า e2e (U07 c_combo_closes · U08 c_modal_no_autoopen) · ยืนยันว่ายังไม่แก้ต้นทาง · **⟳ เกิดซ้ำ F-HR-Performance 2026-09-09** (e2e DSP-02b จับได้ — modal เปิดแล้ว combobox กางเอง · แก้สำเนา `outputs/F-HR-Performance/performance.html` guardOverlayAutoCombo หลัง trapFocus ทั้ง openDrawer+openModal เหมือนเดิม · DSP-02a ผ่านอยู่แล้ว) — ยืนยันว่ายังไม่แก้ต้นทาง · เชื่อม [[html-v7-basekit-latent-bugs]] |

