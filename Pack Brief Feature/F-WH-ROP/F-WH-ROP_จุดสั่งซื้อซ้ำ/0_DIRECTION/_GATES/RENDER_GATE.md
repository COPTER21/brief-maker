# S3c — render and freeze

WARN: official v9 render-check was attempted with system Python and the bundled runtime; both reported Playwright unavailable. No desktop/mobile screenshot, typography, sticky table, drawer position, or actual click sequence is certified. The HTML is frozen at the SHA-256 in `HTML_FREEZE_SHA256.txt`. Any later HTML edit requires S3a–S3c rerun plus html-to-frd-sync.

Available evidence is the v9 static audit FAIL=0/WARN=2 and isolated 17/17 domain assertions. Those cannot substitute for browser rendering.
