# HANDOFF — Stocktake · F-WH-STOCKTAKE

Phase A direction for Warehouse large-count console. S3c browser gate completed with WARN (core count/recount/DOA/mock handoff PASS; new-round overlap and 1024 visual check remain for vibe).

## ทำอะไร / ส่งมอบอะไร

Selected Warehouse/Location scope with [ASSUMED OQ-ST-01] scope-only movement freeze and immutable snapshot at timestamp, assigned blind count [ASSUMED OQ-ST-02], threshold config/effective-date independent recount, supervisor variance, named-person DOA, append-only audit and W3-LITE Stock Adjustment soft-link mock [ASSUMED contract]. `0_DIRECTION` carries PREBRIEF (13 scenarios/12 FN), FN MD+interactive HTML, ERP BASELINE and this HANDOFF. `1_HTML` carries v9 console and S3 reports. `2_BRD` carries MD/DOCX and AI gate. `5_DECLARATIONS` contains exactly doa+csq (2/2); no doccfg/pdfdoc.

## ห้ามแตะ

No Pattern Q, document number/PDF, Cycle Count ABC W4-FULL, Item/Warehouse master implementation, direct inventory balance change, hardcoded DOA chain, hard delete, FRD/TC S5/S6. W3-LITE Adjustment and inventory movement engine remain mocks/hooks. Preserve selected-scope freeze, snapshot timestamp, append-only count/decision/movement events.

## ASSUMED/OQ + owner

OQ-ST-01 scoped freeze (Warehouse + Inventory engine), OQ-ST-02 blind count/supervisor reveal (Warehouse + Policy), W3-LITE handoff payload/ack [ASSUMED contract] (Warehouse integration). [AI-DRAFT] nested scope overlap, threshold equality and release timing (Warehouse BA). DIVERGENCE-GRAPH-01 generic validator flags intentionally edge-free LITE graph (pack maintainer). UI WARN typography/loading (UI team) and DOCX polish (BA). #105 DEMO persona switcher in HTML is review harness only.

## Vibe / Phase B

Team may refine console interactions only within the locked business rules and test counter/supervisor distinction, recount, DOA and scoped freeze at desktop/1024. **รอทีม vibe — Phase B ออกเอกสารหลัง HTML นิ่ง + ผ่าน re-gate html-review-fix-order**. Phase B FRD and UI Brief must each contain **“Demo-only elements”** section identifying the `data-demo="persona-switch"` + DEMO badge as non-production; TC must mark role switching as demo harness. Only after re-gate create FRD/UI Brief/TC. UI #67.1/#104/#105/#106 are BLOCK at re-gate.
