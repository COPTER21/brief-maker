# Prompt — ตรวจคุณภาพ UI ด้วย Playwright

> ฉบับ 2026-08-05 · sync กับ `uikit.py` (773 บรรทัด) ที่อยู่โฟลเดอร์เดียวกัน
> **แก้ `uikit.py` เมื่อไหร่ ให้มาอัปไฟล์นี้ด้วย** — วางไว้คู่กันเพื่อไม่ให้ drift เงียบ ๆ
>
> ที่มา: กลั่นจากการตรวจจริง 5 feature — ตัวเลขทุกตัวใน "เคสจริง" คือของที่เกิดขึ้นแล้ว ไม่ใช่ตัวอย่างสมมติ

---

## 🔑 อ่านก่อน — prompt นี้ใช้ **ครั้งเดียวต่อโปรเจกต์** ไม่ใช่ทุกครั้งที่ตรวจ

| | โหมด A — **ครั้งแรกของโปรเจกต์** | โหมด B — **ทุกครั้งหลังจากนั้น** |
|---|---|---|
| **ทำอะไร** | สร้าง "ตัวตรวจกลาง" จาก prompt ข้างล่าง | เขียนแค่ **"ลำดับการเดินจอ"** ของหน้านั้น |
| **เขียนกี่บรรทัด** | ~750 | **~160** (2 ฟังก์ชัน) |
| **ทำบ่อยแค่ไหน** | **ครั้งเดียว** | ทุก feature |
| **trigger** | *"ตั้งตัวตรวจ UI ตาม PROMPT_ui-audit.md"* | *"ตรวจ UI ของ &lt;feature&gt;"* |

### โหมด B ต้องเขียนแค่ 2 ฟังก์ชัน

```python
from uikit import Audit          # ← ตัวตรวจกลาง 6 ด้าน มาให้หมดแล้ว

def walk_layout(a, pg, tag):     # เปิดจอไหนบ้าง กางลิ้นชักไหน คลิกแท็บไหน
    ...                          # แล้วเรียก a.scan(pg, tag) ทุกฉาก

def walk_behavior(a, pg):        # เดิน flow: submit · Esc · ปิด overlay ซ้อน
    ...
```

**ห้ามเขียนตัวตรวจใหม่** ถ้าโปรเจกต์มีอยู่แล้ว — เจอจุดที่ตัวตรวจกลางมองไม่เห็น
ให้**เพิ่มเข้าตัวตรวจกลาง** (ทุก feature ได้ประโยชน์) ไม่ใช่แปะเฉพาะไฟล์นี้

> 📍 **โปรเจกต์ CUBE อยู่โหมด B แล้ว** — ตัวตรวจกลางคือ `uikit.py` ข้าง ๆ ไฟล์นี้
> ตัวอย่างที่ทำจบ: `_final-docs/F-COA-001/_e2e/ui-audit-coa.py` (157 บรรทัด)
> **prompt ข้างล่างจึงไม่ต้องอ่านทุกรอบ** — ใช้ตอนตั้งของใหม่ หรือตอนจะเติมการตรวจเข้า `uikit.py`

---

```
ตรวจคุณภาพ UI ของไฟล์/หน้าเว็บนี้ให้ครอบคลุม 6 ด้าน โดยเขียนสคริปต์ตรวจอัตโนมัติ
แล้วรันจริงด้วย Playwright — ห้ามสรุปจากการอ่านโค้ด

1. Layout — ใช้ getBoundingClientRect ตรวจ:
   • element ถูก overflow ของกล่องแม่ตัด (clipping)
   • scroll แนวนอนที่ไม่ตั้งใจ
   • ปุ่ม/input ในแถวเดียวกันสูงไม่เท่ากัน
   • ข้อความล้นกล่อง
   • element ซ้อนทับกัน
   • เนื้อหาในเซลล์ตารางล้นออกนอกคอลัมน์ไปทับคอลัมน์ข้าง ๆ
     (มักเกิดจาก min-width ของ div ข้างในชนะความกว้างคอลัมน์)
   • ช่องกรอกในแถวตารางเดียวกันขอบบนไม่ตรงกัน
     (สูงเท่ากันแต่คนละระดับ — เกิดเมื่อบางเซลล์มีป้าย/คำอธิบายซ้อนใต้ช่องกรอก)

   • ★ ของอื่นวาดทับลิ้นชัก/หน้าต่างที่เปิดอยู่ — ข้อที่สำคัญที่สุดในหมวดนี้
     ยิง elementFromPoint 8 จุดกระจายทั่ว overlay ที่เปิดอยู่ ถ้าจุดไหนคืน
     element ที่ไม่ได้อยู่ใน overlay = มีของทะลุขึ้นมาทับ
     ⚠️ หัวตาราง sticky เป็นตัวการที่พบบ่อยที่สุด
     ⚠️ ข้อ "element ซ้อนทับกัน" ข้างบนจับไม่ได้ เพราะมันเทียบเฉพาะพี่น้อง
     ใน flex/grid box เดียวกัน ส่วนหัวตารางกับลิ้นชักอยู่คนละ subtree

   • ★ CSS var ที่ถูก var() เรียกใช้ แต่ไม่มีใครประกาศค่าใน :root
     สแกน CSS ทั้งไฟล์: เก็บ /(--[\w-]+)\s*:/ เป็น "ประกาศแล้ว"
     เทียบกับ /var\((--[\w-]+)/ ที่ "เรียกใช้" แล้วรายงานตัวที่หาย
     ⚠️ ตัวที่หายจะ computed เป็น auto/ค่าว่าง — จอไม่พังทันทีจึงไม่มีใครสังเกต
     แต่ถ้าเป็นชุด --z-* ทั้งระบบจะกลายเป็น z-index:auto พร้อมกัน
     (เคสจริง 2026-08-04: นี่คือต้นเหตุของข้อ "วาดทับลิ้นชัก" ข้างบน)

   • ★ class ที่ markup ใช้ แต่ไม่มีนิยามอยู่ใน CSS
     → สไตล์เงียบหาย · เคสร้ายคือ element ที่ควรถูกซ่อนกลับโผล่ค้าง
     (เคสจริง: .hidden หายไปจากแม่แบบ ทำให้เมนูค้นหาเปิดค้าง + ปุ่ม X โผล่)
     รายงานคู่กับ "ตัวควบคุมมองไม่เห็น" เพื่อชี้ว่าอาการมาจากสาเหตุนี้

   • ★ ค่าที่ตั้งใน CSS ไม่มีผลเพราะ element เป็น display:inline
     width / height / margin แนวตั้ง ไม่ทำงานบน inline ตามสเปก
     — ต้องเตือน ไม่ใช่ปล่อยผ่านเพราะ "ไม่มี error"

   • ★ เมนู/dropdown ที่กางอยู่ ถูกของอื่นวาดทับ (คนละเคสกับ overlay ข้างบน)

   • ★ หัวลิ้นชัก/หน้าต่าง: ปุ่มปิดต้องอยู่ขวาสุด แถวเดียวกับหัวข้อ

   • คำอธิบายที่เด้งตอนชี้ (tooltip) — ต้องตรวจ 3 อาการคู่กันเสมอ:
       ก) ถูกกล่องแม่ตัด   ข) ล้นออกนอกจอ   ค) ชี้แล้วไม่มีอะไรโผล่เลย
     ⚠️ ข้อ ค) ขาดไม่ได้ — ถ้าวันหนึ่ง tooltip พังจนไม่ขึ้น ตัววัดที่มีแค่ ก)+ข)
     จะรายงาน "ผ่าน" เพราะไม่มีของให้ตรวจ
     ⚠️ tooltip ที่วาดด้วย ::before/::after เป็น pseudo-element — querySelectorAll
     หาไม่เจอและวัดขนาดไม่ได้ ต้องก๊อป computed style ของ pseudo ลง element
     ชั่วคราวแล้วค่อยวัด · แบบที่วาดด้วย JS ตอน hover ให้ยิง pointerover ก่อนวัด

2. Responsive — รันซ้ำทุกความกว้าง 1280 / 1440 / 1920 / 2560

3. Cross-browser — Chromium / Firefox / WebKit
   • ถ้าเป็นแอปที่ส่งมอบจริง = บังคับทุกรอบ
   • ถ้าเป็นต้นแบบ/ไฟล์อ้างอิง = ทำเป็นตัวเลือก เปิดเมื่อ CSS พื้นฐานเปลี่ยน
     (กินเวลา ~2 ใน 3 ของการตรวจทั้งหมด — บอกผู้ใช้ก่อนเปิด)
   • เครื่องที่ลง Chromium ไว้อย่างเดียว ต้อง `playwright install firefox webkit`
     ก่อน (~3 นาที) — บอกผู้ใช้ก่อนเริ่ม อย่าให้รอเงียบ ๆ

4. Behavior — เดินทุก flow ให้จบ: เปิด-ปิด overlay ทุกตัวสองรอบ · คลิกทุกแท็บ ·
   submit ทุกฟอร์ม · Esc ทีละชั้น · refresh ทุก route รวม route ที่ไม่มีจริง
   • ★ ปิดหน้าต่างยืนยันแล้วปิดลิ้นชักติดกันทันที → ต้องไม่มีของค้างทับจอ
     (scroll lock / backdrop ที่ไม่ถูกปลด ทำให้กดอะไรไม่ได้ทั้งหน้า)

5. Runtime — console error / warning / pageerror ต้องเป็น 0
   แยกข้อความที่มาจาก CDN ภายนอกออกก่อน โดยดูจาก URL ของ request จริง
   (ดัก event ของ response / requestfailed) ไม่ใช่ดูจากข้อความ

6. Accessibility — ทุก element ที่คลิกได้ต้องโฟกัสด้วยคีย์บอร์ดได้และเห็นกรอบโฟกัส ·
   Enter/Space ต้องทำงานเหมือนคลิก · Tab เดินครบและถูกลำดับ · contrast ผ่าน WCAG AA

7. ภาพหน้าจอต้องให้ผลเดิมทุกรอบ (ถ้าเก็บภาพไว้เทียบ)
   • สั่งหยุด CSS animation ก่อนกดชัตเตอร์ (Playwright: animations="disabled")
   • พิสูจน์ด้วยการรัน 2 รอบลงคนละโฟลเดอร์ แล้วเทียบว่าได้ไฟล์เหมือนกันทุกไบต์
   ⚠️ ไม่ทำ = ภาพจะ "เปลี่ยน" ทุกรอบเพราะจับ animation คนละจังหวะ (เคสจริง:
   จุดกะพริบขนาด 26px ทำให้ 24 จาก 27 ภาพต่างกันทุกครั้ง — ต่างแค่ 0.010%
   ของภาพ แต่ทำให้รีวิวภาพใช้ไม่ได้ เพราะของที่เปลี่ยนจริงจมอยู่ในเสียงรบกวน)
   ผลพลอยได้: หน้าต่าง/ลิ้นชักจะถูกจับตอนเฟดจบแล้ว ไม่ใช่กลางทาง

═══ สัญญาณหลอก 12 แบบที่พิสูจน์แล้ว — กันไว้ในตัวตรวจตั้งแต่แรก อย่าไปไล่ ═══
 1 กล่องที่ scroll ได้ ≠ ถูกตัด (เลื่อนไปดูได้) — ตัดจริงเมื่อ overflow:hidden
   หรือเป็นเมนูลอย (absolute/fixed) ที่ตกอยู่ในกล่อง scroll
   ยกเว้น: tooltip ไม่ใช่เนื้อหาที่เลื่อนไปดูได้ → กล่อง scroll ที่ตัด tooltip = ของจริง
 2 element ที่ display:inline คืน clientWidth = 0 ตามสเปก — วัดข้อความล้นไม่ได้
 3 พื้นไล่สี (gradient) อ่านค่าสีพื้นไม่ได้ — ข้าม contrast
 4 onclick="event.stopPropagation()" ไม่ใช่ปุ่ม — ไม่ต้องโฟกัสได้
 5 ฉากหลัง overlay (backdrop) ไม่ต้องโฟกัสได้ — ปิดด้วย Esc/ปุ่ม X อยู่แล้ว
 6 ข้อความ "Failed to load resource" ไม่มี URL — ต้องดูจาก event ของ request จริง
 7 ตัวเลือกในเมนูที่กางอยู่ ไม่ใช่ control ในแถว
 8 ปุ่มขนาดเล็ก (btn-sm) ในโซนตัวกรอง สูงต่างจาก input ได้ตามสเปก
 9 กล่องที่คลิกได้ ถ้ามี control โฟกัสได้อยู่ข้างในซึ่งเรียก "ฟังก์ชันชื่อเดียวกัน"
   = คีย์บอร์ดทำสิ่งนั้นได้แล้วผ่าน control ตัวนั้น (เช่น แถวตารางคลิกกาง +
   ปุ่มลูกศรในแถว) — ต้องเทียบชื่อฟังก์ชันเป๊ะ ๆ control ที่ทำคนละเรื่องไม่นับ
10 แถวที่เป็น "แผงกางออก" (เซลล์เดียว colspan หลายช่อง) ข้างในเป็น grid หลายบรรทัด
   → ห้ามตรวจ "ขอบบนตรงแนว" กับแถวแบบนี้ (แต่ยังตรวจ "สูงเท่ากัน" ตามปกติ)
11 ★ ในแถวตาราง ถ้า control อยู่ "คนละ <td>" ให้เทียบ **กึ่งกลางแนวตั้ง**
   ไม่ใช่ "ขอบบน" — control ที่สูงต่างกันแต่จัดกึ่งกลางถูกต้องจะมีขอบบน
   ต่างกันเสมอ (ปุ่ม 20px vs 28px → top ต่าง 4px แต่ center ตรงกันเป๊ะ)
   ตาคนมองที่กึ่งกลาง ไม่ได้มองขอบบน → เทียบ top เฉพาะเมื่ออยู่ "เซลล์เดียวกัน"
   (เคสจริง: รายงาน 248 จุด ไม่มีจุดไหนเพี้ยนจริงสักจุด)
   รวมถึงไอคอนลอยที่จัดกึ่งกลางอยู่ "ใน" ช่องกรอก (ปุ่มล้าง/ลูกศรของช่องค้นหา)
   — ไม่ใช่ control ของแถว ไม่ต้องสูงเท่าช่องกรอก
12 ในแผงกางออก เทียบความสูงทีละบรรทัดที่เห็นจริง (จับกลุ่มด้วยขอบบน)
   ไม่ใช่เทียบทั้งแถวรวด — การ์ด/ลิงก์คนละบรรทัดสูงต่างกันได้

เงื่อนไข:
- รายงานผลเป็น "ตัวเลขที่วัดได้" เช่น "ถูกตัด 270px" ไม่ใช่คำว่า "ผ่าน/ไม่ผ่าน"
- ★ จับภาพไว้แล้ว = ยังไม่ได้ตรวจ · ต้อง "เปิดดูทุกใบ"
  ⚠️ เคสจริง: หัวตารางทะลุลิ้นชัก ผ่านด่านตรวจ 4 ชั้นมาได้ ทั้งที่สคริปต์
  จับภาพฉากนั้นไว้แล้ว ชื่อไฟล์ตรงตัว — แต่ไม่มีใครเปิด ผู้ใช้เป็นคนเจอเอง
  การมีภาพไม่เท่ากับการดูภาพ
- ก่อนแก้ทุกจุด ต้องพิสูจน์ก่อนว่าเป็นข้อบกพร่องจริง ไม่ใช่ตัววัดเขียนผิด
  แล้วรายงานแยกว่า "แก้ที่ไฟล์กี่จุด" · "แก้ที่ตัววัดกี่จุด" · "แก้ที่แม่แบบ/CSS
  ส่วนกลางกี่จุด" · "ต้องให้เจ้าของ design system ตัดสินกี่จุด" พร้อมเหตุผลรายข้อ
- ถ้าเจอเกิน ~30 จุด ให้สงสัยตัววัดก่อนสงสัยไฟล์
  (เคสจริง: เจอ 399 จุด เป็นข้อบกพร่องจริงแค่ 5)
- ถ้าจุดที่ตกอยู่ในโค้ดที่ก๊อปมาจากแม่แบบ/ชุด CSS กลาง — ห้ามแปะแก้เฉพาะไฟล์นี้
  ต้องแก้ที่แม่แบบแล้ว sync กลับ ไม่งั้นไฟล์ถัดไปจะเจอซ้ำและไม่มีใครรู้ว่าเคยแก้แล้ว
  (เคสจริง 3 ครั้ง: บั๊กปุ่ม Esc ที่ทำให้ฟอร์มหายทั้งใบ · แม่แบบที่ทำให้เครื่องตรวจ
  ของตัวเองไม่ผ่าน 8 จุด · แม่แบบที่ไม่ประกาศ --z-* เลยทั้งที่เรียกใช้ 11 จุด)
  · ถ้าแม่แบบเป็นของนอกทีม ให้จดไว้เป็นข้อยกเว้นที่มีวิธีเช็คกลับ (คำสั่ง grep
  พร้อมตัวเลขที่ถูกต้อง) เพราะรุ่นหน้าจะทับทิ้ง
- จุดไหนแก้ไม่ได้เพราะติดกฎ/ติด design token ห้ามแก้เอง — ให้บอกว่าติดอะไร
  กระทบอะไรบ้าง และเสนอค่าที่ต้องเปลี่ยนให้ผ่าน
- ถ้าตัวนับตัวไหนอ่านจาก "โค้ดต้นฉบับ" ไม่ใช่ "ผลที่เรนเดอร์จริง" ให้ระวังว่ามันจะ
  นับโค้ดที่ต่อสตริงเป็นเนื้อหาไปด้วย — เจอแบบนี้ให้ยกข้อความไปเป็นตัวแปรก่อน
  ห้ามตัดข้อความจริงให้สั้นลงเพื่อให้ตัวนับผ่าน
- ถ้าผู้ใช้เจอจุดเพี้ยนที่สคริปต์ตรวจไม่เจอ ห้ามแก้แค่จุดนั้นแล้วจบ —
  ต้องเพิ่มการตรวจแบบนั้นเข้าสคริปต์ด้วย แล้วย้อนโค้ดกลับไปเวอร์ชันที่พัง
  เพื่อพิสูจน์ว่าตัววัดจับได้จริง ก่อนแก้กลับ
- ★ เขียน "ตัวตรวจ" ครั้งเดียวแล้วใช้ตลอดโปรเจกต์ — แยกเป็น 2 ส่วนตั้งแต่แรก
    ก) ตัวตรวจกลาง (ใช้ได้ทุกหน้า ทุก feature) → ไฟล์เดียวที่ทุกคน import
    ข) ลำดับการเดินจอ (ของหน้านี้เท่านั้น) → ~150 บรรทัด เขียนใหม่ต่อ feature
  **ครั้งต่อไปที่ตรวจ ห้ามเขียน (ก) ใหม่** — เขียนแค่ (ข) แล้วเรียกใช้ของเดิม
  เจอจุดที่ (ก) มองไม่เห็น → เพิ่มเข้า (ก) เพื่อให้ feature อื่นได้ด้วย
  ห้ามแปะแก้เฉพาะ (ข) — ไม่งั้นหน้าถัดไปจะหลุดจุดเดียวกันซ้ำ
- เลี่ยงการหน่วงเวลาตายตัว ให้รอเงื่อนไขที่เกิดขึ้นจริง
  ถ้าจำเป็นต้องหน่วง ให้ประเมินเวลารวมมาบอกก่อนเริ่ม
- ★ ⛔ รอคำสั่งก่อนรัน — ห้ามรันเอง
  การตรวจชุดนี้ใช้ Playwright กินเวลา 4–5 นาที ทำให้คนสั่งต้องนั่งรอโดยไม่ได้ขอ
  → เขียนสคริปต์เสร็จให้ "บอกว่าเขียนแล้วแต่ยังไม่รัน" แล้วรอสั่ง
  (ตัวเบาที่จบในวินาทีเดียว — static audit / syntax check — รันได้เลยไม่ต้องถาม)

- ★ สั่งแล้ว **รันรอบเดียว** เครื่องมืออย่างละครั้ง — ห้ามไล่รันวนเอง
  มีอะไรตก → แก้ → รันยืนยัน **อีกรอบเดียว**
  ยังตกอีก → **หยุดแล้วรายงาน** ให้คนตัดสิน อย่ารันรอบที่ 3 เอง
  จะรันมากกว่านี้ต่อเมื่อคนสั่งขอเอง (เช่น "รัน 3 รอบเทียบผล")
  ⚠️ เหตุผลไม่ใช่แค่เวลา — ถ้าแก้แล้วยังตก แปลว่าเข้าใจปัญหาผิด
  การรันซ้ำเรื่อย ๆ คือการเดาไปเรื่อย ๆ ด้วยเงินเวลาของคนอื่น

- ผลต้องไม่แกว่ง — แต่ให้ทำให้ "นิ่งโดยโครงสร้าง" แทนการรันหลายรอบเพื่อสุ่มดู
  (หยุด animation · รอเงื่อนไขจริงแทน sleep · ไม่ใช้เวลา/สุ่มในข้อมูลทดสอบ)
  แล้วยืนยันด้วยการรันซ้ำ 1 รอบว่าได้ผลเท่ากัน — ถ้ายังแกว่ง แปลว่ายังมีแหล่ง
  ความไม่แน่นอนที่ยังไม่เจอ ให้ตามหาให้เจอ อย่าแก้ด้วยการรันเพิ่มรอบ
- บอกด้วยว่าอะไรที่สคริปต์ตรวจไม่ได้ และต้องให้คนดูเอง
  (ความสวยงาม · ลำดับการใช้งาน · คำอธิบายเข้าใจมั้ย · screen reader)
```

---

## หมายเหตุสำหรับคนที่รับ prompt นี้ไปใช้

**★ = ของที่เพิ่มรอบ 2026-08-05** หลังเจอบั๊กจริงที่ prompt ฉบับแรกจับไม่ได้

| ★ ที่เพิ่ม | เจอจากอะไร |
|---|---|
| ของอื่นวาดทับ overlay · CSS var ไม่ประกาศ | หัวตาราง sticky ทะลุลิ้นชัก — ผ่านด่านตรวจ 4 ชั้น ผู้ใช้เจอเอง |
| class ไม่มีนิยามใน CSS | `.hidden` หายจากแม่แบบ → เมนูค้นหาเปิดค้าง |
| CSS ไม่มีผลเพราะ inline | ตั้งค่าแล้วเงียบ ไม่มี error ให้จับ |
| ลำดับการปิด overlay | ปิด modal + drawer ติดกัน แล้ว scroll lock ค้าง |
| FP#11 เขียนใหม่ | เทียบขอบบนแทนกึ่งกลาง → 248 จุดปลอม |
| "จับภาพ ≠ ตรวจ" | บทเรียนที่แพงที่สุด — มีภาพแล้วไม่เปิดดู |
| **รอคำสั่ง + รันรอบเดียว** | ฉบับแรกเขียนว่า *"รันซ้ำจนเป็นศูนย์"* ซึ่ง**ขัดกับกติกาจริง** — AI จะไล่รันวนเอง รอบละ 4–5 นาที |

**ถ้าจะเอาไปใช้กับโปรเจกต์อื่น:** ส่วนที่ผูกกับ CUBE คือชื่อ class ในตัวอย่าง
(`.lt` `.ss-list` `.dw-footer` `btn-sm`) — เปลี่ยนเป็นของโปรเจกต์ตัวเองได้
ส่วนที่เหลือเป็นกลไกทั่วไปใช้ได้ทุกที่

**ตัวจริงที่ implement prompt นี้แล้ว:** `uikit.py` (โฟลเดอร์เดียวกัน) — แยก
"ตัวตรวจ" ออกจาก "ลำดับการเดินจอ" ตามที่ prompt กำหนด · แต่ละ feature เขียนแค่
`walk_layout` / `walk_behavior` ของตัวเอง (ดูตัวอย่าง `_final-docs/F-COA-001/_e2e/ui-audit-coa.py`)